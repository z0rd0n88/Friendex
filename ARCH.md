# Architecture Map — Friendex

> **Auto-maintained.** The file tree below (between the `ARCH:TREE` markers) is regenerated on every commit by `.githooks/pre-commit` and can be refreshed manually with `python3 .githooks/gen_arch.py`. The **Overview** and **Key Paths** sections are curated (fill them with the `code-explorer` agent); they are preserved across regenerations. Descriptions in *Key Paths* are injected inline into the tree.

## Overview

Friendex is a Python Discord bot that simulates a per-guild stock exchange: each server member has a tradeable stock whose price moves in response to real Discord activity (messages, voice time, reactions). It is a greenfield hexagonal (ports-and-adapters) rebuild under `src/friendex/`, managed with `uv` and targeting Python 3.11+. The architecture flows inward: `adapters/` (discord_bot, persistence via async SQLAlchemy + SQLite, config via pydantic-settings) → `application/` (use-case services) → `domain/` (pure dataclass models and error taxonomy, no external imports). The entry point will be `friendex.main:main` (wired in a later phase); until then the exercisable surface is the test suite (`uv run pytest`). CI runs ruff, mypy --strict, and pytest across Python 3.11 and 3.12 on every push and PR.

## Key Paths

<!-- ARCH:DESC:START — curated; one line per key path, preserved across regens -->
- `.claude/` — project-scoped Claude Code configuration: custom agents (code-explorer, python-reviewer, sql-pro), skills (pass-baton, baton-runner), memory notes, and settings.json.
- `.githooks/` — shared git hook scripts: `pre-commit` regenerates ARCH.md's file-tree map before every commit, and `gen_arch.py` is the generator it calls.
- `.github/` — GitHub Actions CI workflow (lint/type/test matrix on Python 3.11 and 3.12) and the pull request body template.
- `docs/` — curated project documentation: architecture docs (current state, target, migration plan, testing strategy), ADRs, product ideas, prompts, and code-review records.
- `pass-baton/` — append-only session handoff log organized by feature/epic subdirectory; its `INDEX.md` points new sessions to the latest context for each in-flight phase.
- `src/` — the `friendex` package root containing all production source code, organized into `domain/`, `application/`, and `adapters/` layers.
- `tests/` — pytest test suite mirroring the `src/` layer structure, with fixtures and a conftest for shared test setup.
- `.env.example` — template listing required and optional environment variables (`DISCORD_TOKEN`, `DEV_GUILD_ID`, database URL, game tunables) for local development.
- `.gitattributes` — git attributes file controlling line-ending and diff behavior for files in the repository.
- `.gitignore` — specifies files and directories excluded from version control (virtualenvs, build artifacts, `.env`, data files, etc.).
- `.pre-commit-config.yaml` — configuration for the pre-commit framework declaring hooks that run ruff and other checks locally before commits.
- `.python-version` — pins the Python version used by `uv` and pyenv for consistent local interpreter selection.
- `CLAUDE.md` — project-level Claude Code instructions covering architecture layers, development commands, repo workflow rules, game rules, and phase status conventions.
- `mypy.ini` — mypy static type-checker configuration enabling strict mode for the `src/friendex` package.
- `pyproject.toml` — canonical project manifest: package metadata, runtime dependencies, dev dependencies, and tool configuration for pytest and coverage.
- `README.md` — top-level project introduction with a brief description and the core development commands for new contributors.
- `ruff.toml` — ruff linter and formatter configuration: line length, target Python version, enabled rule sets, and per-file overrides.
- `uv.lock` — locked dependency manifest generated by `uv`, pinning exact transitive versions for reproducible installs.
<!-- ARCH:DESC:END -->

## File Tree

<!-- ARCH:TREE:START — generated by gen_arch.py; do not edit inside, do not remove -->
```
br-2026-05-23-p4p5/
├── .claude/  — project-scoped Claude Code configuration: custom agents (code-explorer, python-reviewer, sql-pro), skills (pass-baton, baton-runner), memory notes, and settings.json.
│   ├── agents/
│   │   ├── code-explorer.md
│   │   ├── ecc-code-simplifier.md
│   │   ├── ecc-python-reviewer.md
│   │   ├── python-pro.md
│   │   └── sql-pro.md
│   ├── memory/
│   │   └── friendex-migration-roadmap.md
│   ├── skills/
│   │   ├── baton-runner/
│   │   │   ├── scripts/
│   │   │   │   └── gate.sh
│   │   │   ├── REFERENCE.md
│   │   │   └── SKILL.md
│   │   ├── ecc-python-patterns/
│   │   │   └── SKILL.md
│   │   └── pass-baton/
│   │       └── SKILL.md
│   └── settings.json
├── .githooks/  — shared git hook scripts: `pre-commit` regenerates ARCH.md's file-tree map before every commit, and `gen_arch.py` is the generator it calls.
│   ├── gen_arch.py
│   └── pre-commit
├── .github/  — GitHub Actions CI workflow (lint/type/test matrix on Python 3.11 and 3.12) and the pull request body template.
│   ├── workflows/
│   │   └── ci.yml
│   └── pull_request_template.md
├── baton-runner/
│   └── br-2026-05-23-p4p5/
│       ├── log.md
│       └── STATE.md
├── docs/  — curated project documentation: architecture docs (current state, target, migration plan, testing strategy), ADRs, product ideas, prompts, and code-review records.
│   ├── adr/
│   │   ├── 0001-per-guild-markets.md
│   │   └── README.md
│   ├── product/
│   │   └── 2026-05-13-engagement-ideas.md
│   ├── prompts/
│   │   ├── 2026-05-13-friendex-python-refactor-plan.md
│   │   └── 2026-05-13-parallel-phase-1-scaffold-and-engagement-brainstorm.md
│   ├── reviews/
│   │   └── pr-11-team-review.md
│   ├── spec/
│   │   └── original-skeleton.md
│   ├── 01-current-state.md
│   ├── 02-target-architecture.md
│   ├── 03-python-review.md
│   ├── 04-migration-plan.md
│   ├── 05-testing-strategy.md
│   └── 06-per-guild-markets-migration.md
├── pass-baton/  — append-only session handoff log organized by feature/epic subdirectory; its `INDEX.md` points new sessions to the latest context for each in-flight phase.
│   ├── phase-3-domain/
│   │   ├── 000-2026-05-15-phase-3-kickoff.md
│   │   └── 001-2026-05-15-phase-3-pr11-ready.md
│   ├── phase-4-domain-funcs/
│   │   ├── 000-2026-05-22-phase-4-kickoff.md
│   │   └── 001-2026-05-23-price-engine-activity-done.md
│   ├── 000-2026-05-15-start-pass-baton.md
│   ├── CLAUDE.md
│   └── INDEX.md
├── scripts/
│   └── gate.sh
├── src/  — the `friendex` package root containing all production source code, organized into `domain/`, `application/`, and `adapters/` layers.
│   └── friendex/
│       ├── adapters/
│       │   ├── discord_bot/
│       │   │   ├── cogs/
│       │   │   │   └── __init__.py
│       │   │   ├── listeners/
│       │   │   │   └── __init__.py
│       │   │   └── __init__.py
│       │   ├── persistence/
│       │   │   └── __init__.py
│       │   ├── tasks/
│       │   │   └── __init__.py
│       │   ├── __init__.py
│       │   └── config.py
│       ├── application/
│       │   └── __init__.py
│       ├── domain/
│       │   ├── __init__.py
│       │   ├── activity.py
│       │   ├── errors.py
│       │   ├── models.py
│       │   └── price_engine.py
│       └── __init__.py
├── tests/  — pytest test suite mirroring the `src/` layer structure, with fixtures and a conftest for shared test setup.
│   ├── adapters/
│   │   ├── fixtures/
│   │   │   └── test.env
│   │   ├── __init__.py
│   │   └── test_config.py
│   ├── domain/
│   │   ├── __init__.py
│   │   ├── conftest.py
│   │   ├── test_activity.py
│   │   ├── test_errors.py
│   │   ├── test_models.py
│   │   └── test_price_engine.py
│   ├── __init__.py
│   ├── conftest.py
│   └── test_scaffold.py
├── .env.example  — template listing required and optional environment variables (`DISCORD_TOKEN`, `DEV_GUILD_ID`, database URL, game tunables) for local development.
├── .gitattributes  — git attributes file controlling line-ending and diff behavior for files in the repository.
├── .gitignore  — specifies files and directories excluded from version control (virtualenvs, build artifacts, `.env`, data files, etc.).
├── .pre-commit-config.yaml  — configuration for the pre-commit framework declaring hooks that run ruff and other checks locally before commits.
├── .python-version  — pins the Python version used by `uv` and pyenv for consistent local interpreter selection.
├── ARCH.md
├── CLAUDE.md  — project-level Claude Code instructions covering architecture layers, development commands, repo workflow rules, game rules, and phase status conventions.
├── mypy.ini  — mypy static type-checker configuration enabling strict mode for the `src/friendex` package.
├── pyproject.toml  — canonical project manifest: package metadata, runtime dependencies, dev dependencies, and tool configuration for pytest and coverage.
├── README.md  — top-level project introduction with a brief description and the core development commands for new contributors.
├── ruff.toml  — ruff linter and formatter configuration: line length, target Python version, enabled rule sets, and per-file overrides.
└── uv.lock  — locked dependency manifest generated by `uv`, pinning exact transitive versions for reproducible installs.
```

_Generated 2026-05-23 18:40 UTC · 77 files · 37 directories._
<!-- ARCH:TREE:END -->
