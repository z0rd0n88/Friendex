# Pass-Baton: Phase 8 follow-up review — VERDICT CLEAN

**Date:** 2026-05-25
**Scope:** phase-8-followup
**Branch:** feat/phase-8-followup-cooldown-and-k
**Worktree:** /home/alex/Friendex/.claude/worktrees/br-2026-05-25-phase-8
**HEAD:** b01480b chore(config): calibrate activity_tick_k 0.5 -> 0.3 (8b M1 follow-up)

## Where things stand

Reviewed the two-chore follow-up unit (chore A = 8c M2 cooldown Protocol
widening at `68c1a8f`; chore B = 8b M1 `activity_tick_k` calibration at
`b01480b`). Gate `baton-runner/br-2026-05-25-phase-8/gate-phase-8-followup-iter-1/`
is green (pytest, ruff-check, ruff-format, mypy all PASS). Diff against
`feat/phase-8f-liq-disc` covers two product files, three test files, two
baton-pass additions, and an auto-regenerated `ARCH.md`. No dependency
changes (`pyproject.toml` / `uv.lock` untouched). Both chores meet intent;
no CRITICAL, HIGH, or MEDIUM issues. **VERDICT: CLEAN.**

## Findings by severity

### CRITICAL / HIGH

None.

### MEDIUM

None. The MEDIUM contract-docstring concern raised in the prompt is
satisfied: `ITradeCooldownRepo.get`'s docstring
(`src/friendex/application/interfaces.py:244-256`) explicitly states the
active-vs-expired semantic ("a row is considered expired (and excluded) once
`expires_at <= now`") and justifies why `now` is keyword-only and required
rather than wall-clock-derived.

### LOW / NOTE

1. **`activity_tick_k` typed as `float`, not `Decimal`.** The prompt's "verify
   chore B" section suggested `Decimal("0.3")`; the implementation keeps the
   existing `float = 0.3` (was `float = 0.5`). This matches the field's prior
   type and the project's Decimal-only-for-money invariant (K is a domain
   coefficient, not a money quantity), so it is not a violation — flagging
   only because the prompt phrased it ambiguously. No action required.
2. **One activity-tick assertion still uses `>= marker` rather than deriving
   the exact post-tick value from `default_settings.activity_tick_k`** (line
   627 of `tests/application/test_price_tick_service.py`). This is the
   prompt's preferred (a) form — K-agnostic comparator, not a hard-coded K=0.3
   literal — so it does NOT trigger the LOW the prompt called out. Recorded
   here as confirmation, not as a finding.
3. The `Decimal("96.00")` assertion at line 268 of
   `test_price_tick_service.py` is the **inactivity-decay** path (`100 *
   (1 - 0.04)`), not the activity-K path — independent of this change.

## Intent verification

### Chore A — Protocol widening (commit `68c1a8f`)

- `ITradeCooldownRepo.get` Protocol signature at
  `src/friendex/application/interfaces.py:244-246` is
  `get(self, guild_id: str, user_id: str, *, now: datetime) -> TradeCooldown | None`
  — keyword-only, required. **Confirmed.**
- `SqlTradeCooldownRepository.get` at
  `src/friendex/adapters/persistence/cooldown_repo.py:70-72` matches; the
  `None` default was dropped, `datetime.now(UTC)` fallback removed, `UTC`
  import moved to `TYPE_CHECKING`. **Confirmed.**
- `FakeTradeCooldownRepo.get` at `tests/application/fakes/fake_repos.py:254-256`
  matches; the kwarg actually filters — line 264 returns `None` when
  `cooldown.expires_at <= now`, behaviourally verified by the new
  `test_cooldown_repo_get_returns_none_for_expired_row` test (active row
  returned; expired row → None; missing row → None). **Confirmed.**
- `TradingService._check_cooldown` at
  `src/friendex/application/trading_service.py:185` now passes `now=now`
  and dropped the compensating `if remaining <= 0: return` guard (diff
  shows the removal at lines 188-189 of the pre-image). **Confirmed.**
- RED-first artefact:
  `tests/application/test_interfaces.py::test_cooldown_repo_get_declares_keyword_only_now`
  uses `inspect.signature(ITradeCooldownRepo.get)` and asserts both that
  `"now" in sig.parameters` AND that it is `Parameter.KEYWORD_ONLY`. Against
  the prior narrower Protocol (`get(self, guild_id, user_id)`), the first
  assertion would fail with the documented message. **Genuinely
  load-bearing.** The behavioural twin
  `test_cooldown_repo_get_returns_none_for_expired_row` doubles the coverage
  to the semantic half. **Confirmed.**
- Architecture / contract check: only three in-tree implementations of
  `ITradeCooldownRepo` exist (`SqlTradeCooldownRepository`,
  `FakeTradeCooldownRepo`, and the inline `_FakeCooldownRepo` in
  `test_interfaces.py`); all updated. `liquidation_service.py` types
  `cooldown_repo: ITradeCooldownRepo` but never calls `.get()` (background
  sweeps will use `purge_expired` only), so no migration needed there. No
  out-of-tree consumer to worry about. **Backward-incompatible change is
  internally consistent. Confirmed.**
- No 8c trading-service regressions: full gate pytest = 533 passing.
  **Confirmed.**

### Chore B — `activity_tick_k` calibration (commit `b01480b`)

- `activity_tick_k` default at `src/friendex/adapters/config.py:108` is now
  `float = 0.3` (was `0.5`). **Confirmed.**
- Docstring at `src/friendex/adapters/config.py:94-107` no longer says
  "TBD/placeholder"; it now opens with "Initial calibration (2026-05-25)",
  documents the K=0.5 runaway (~+560%/day at 15 units/tick), justifies the
  K=0.3 choice, and pins re-tuning to Phase 9 against representative
  hourly buckets. **Confirmed.**
- Tests in `tests/application/test_price_tick_service.py` were not relaxed
  to ranges; every activity-tick assertion uses relative comparators
  (`>`/`<`/`>=`/`== starting`/`>= marker`) that hold for any positive K.
  The one updated comment at line 622-625 swapped "positive return at k=0.5"
  for "positive return for any positive `activity_tick_k` (default K=0.3)"
  — phrasing aligns with the prompt's preferred (a) form (K-agnostic).
  **Confirmed.**
- Full app suite still green (533 passing per gate). **Confirmed.**

## PR scope hygiene

- Two commits, cleanly separable: chore A touches `interfaces.py`,
  `cooldown_repo.py`, `trading_service.py`, and the three cooldown test
  files; chore B touches `config.py` and `test_price_tick_service.py`.
  Zero file overlap on product/test code → either commit can be reverted
  independently without dragging the other. **Confirmed.**
- Commit messages match the work-unit templates exactly (subject + body +
  `Refs #2 (<8c M2|8b M1> follow-up)` footer). **Confirmed.**

## Gate output

```
=== gate: pytest ($*: uv run pytest) ===
PASS pytest
=== gate: ruff-check ($*: uv run ruff check src tests alembic) ===
PASS ruff-check
=== gate: ruff-format ($*: uv run ruff format --check src tests alembic) ===
PASS ruff-format
=== gate: mypy ($*: uv run mypy src/friendex) ===
PASS mypy
----
GATE: PASS
```

Log path: `baton-runner/br-2026-05-25-phase-8/gate-phase-8-followup-iter-1/`

## Next steps

1. Manager pushes `feat/phase-8-followup-cooldown-and-k` and opens the PR.
2. No fix iteration needed — verdict CLEAN.

## References

- Issue: #2 (Phase 8 tracker, 8c M2 + 8b M1)
- Diff base: `feat/phase-8f-liq-disc`
- Commits reviewed:
  - `68c1a8f` refactor(application): widen ITradeCooldownRepo.get with kwarg-only now=
  - `b01480b` chore(config): calibrate activity_tick_k 0.5 -> 0.3 (8b M1 follow-up)
- Predecessor batons:
  - [`000-2026-05-25-cooldown-protocol-widened.md`](./000-2026-05-25-cooldown-protocol-widened.md)
  - [`001-2026-05-25-activity-k-calibrated.md`](./001-2026-05-25-activity-k-calibrated.md)
- Originating review batons:
  - `baton-pass/phase-8c/001-2026-05-25-review-iter-1-clean.md` §M2
  - `baton-pass/phase-8b/004-2026-05-25-phase-8b-review-iter2-clean.md`
