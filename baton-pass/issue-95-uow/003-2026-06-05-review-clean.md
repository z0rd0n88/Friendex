# Pass-Baton: Independent review — VERDICT CLEAN

**Date:** 2026-06-05
**Scope:** issue-95-uow
**Branch:** fix/liquidation-uow
**Worktree:** /home/alex/Friendex/.claude/worktrees/issue-95-uow
**HEAD:** 6ed91b3 fix(application): wrap LiquidationService cover_forced in UoW envelope

## Where things stand

Independent review of phase 1 iteration 1 (commit `6ed91b3` against
`origin/main` at `3d8683e`) — issue #95: LiquidationService UoW envelope
wrap. Reviewed the four product files in scope (no docs/baton noise) plus
the RED capture in baton 001 and the existing UoW infrastructure
(`unit_of_work.py`, `FakeUnitOfWork`, the reference `_ExplodingPriceRepo`
template at `tests/application/test_trading_service_atomicity.py:300`).

**Objective gate:** `scripts/gate.sh
baton-runner/issue-95-uow/gate-phase-1-iter-1/` → **PASS** (pytest 1061
passed; ruff-check clean; ruff-format clean; mypy clean across 75 source
files).

**Acceptance criteria — all three satisfied.**

- **AC1 (UoW envelope wraps cover_forced)** — verified.
  `LiquidationService.__init__` at `liquidation_service.py:78` takes
  kw-only `unit_of_work: IUnitOfWork | None = None`, defaulting to
  `NullUnitOfWork()` (lines 95-97), mirroring `TradingService.__init__`
  at `trading_service.py:131-147` (same kw-only position, same default,
  same module-level `NullUnitOfWork` import + `TYPE_CHECKING`
  `IUnitOfWork` import). The `async with self._uow.transaction():` wrap
  at `liquidation_service.py:186` is INSIDE the existing
  `self._locks.locked(...)` block — matches the lock-first /
  transaction-second discipline `cover()` uses at
  `trading_service.py:680-700`. Container wiring at `container.py:467`
  passes `unit_of_work=self._unit_of_work` (the same `SqlUnitOfWork`
  instance already shared with the trading + fund factories).

- **AC2 (rollback regression test pins the wrap)** — verified
  load-bearing. RED evidence in baton 001 is a verbatim
  `TypeError: LiquidationService.__init__() got an unexpected keyword
  argument 'unit_of_work'` from running the new test before any
  production change. New test
  `test_liquidation_rolls_back_on_mid_helper_failure` at
  `tests/application/test_liquidation_service.py:451` uses a local
  `_ExplodingPriceRepo` decorator (line 402, matching the reference
  pattern exactly) and threads a `FakeUnitOfWork` into BOTH services via
  the extended `_make_services` helper (line 119, `unit_of_work` param
  added). Assertions pin all four facets the issue demanded — holder
  cash unchanged, short in full, no LiquidationEvent returned (function
  raised), `uow.rollbacks == 1`. **Mutation check (mental):** stripping
  the `async with self._uow.transaction():` wrap at line 186 makes
  `uow.rollbacks == 1` fail (transaction never entered) AND
  `after_holder.cash_balance == starting_cash` fail (the holder cash
  debit lands via the fake repo's plain dict mutation with no snapshot).
  The test is genuinely pinning the rollback contract, not tautological.

- **AC3 (docstring blocks replaced)** — verified accurate.
  `grep -rn "tracked in issue #95"` over `src/`, `tests/`, `docs/`
  returns no matches. All three replacement one-liners describe the
  post-fix state truthfully:
  - `liquidation_service.py:159-164` correctly enumerates the writes
    rolled back together (target stub, user upsert, fund cash, price
    upsert, history append) and states both call sites now hold the
    envelope.
  - `trading_service.py:769-776` (`_cover_internal`) is symmetric. Minor
    cosmetic LOW: the list includes "cooldown set," which is technically
    a write `cover()` does AFTER `_cover_internal` returns (still inside
    the UoW envelope) rather than inside `_cover_internal`'s body —
    accurate from the envelope's perspective, slightly imprecise about
    which method does the write. Not a regression (the pre-fix docstring
    listed it the same way).
  - `trading_service.py:711-717` (`cover_forced`) correctly states the
    caller-side contract — caller MUST hold the lock AND wrap in a UoW
    envelope. Documentation-only contract, no runtime guard (reasonable
    for an internal-ish entry point with only two known call sites).

**Dependencies:** zero new — `git diff origin/main...HEAD -- pyproject.toml
uv.lock` returns empty.

**Subtle correctness point (architect lens):** the same `SqlUnitOfWork`
instance flows into both `TradingService` and `LiquidationService` via the
container, so when `LiquidationService._maybe_liquidate` enters
`self._uow.transaction()` and then calls `self._trading.cover_forced(...)`,
the inner `_cover_internal` writes ride the same in-flight session
(`SqlUnitOfWork` repos pick up the session via `contextvars` per the
container's design comment at `container.py:189-192`). `cover_forced` is
deliberately a thin delegator at `trading_service.py:729` — it does NOT
open its own UoW — so there is no nested-transaction double-wrap. The
symmetry is correct end-to-end.

**Findings:** 0 CRITICAL, 0 HIGH, 1 MEDIUM (cosmetic — see below), 1 LOW.

| Sev | Location | Description | Fix |
|---|---|---|---|
| M | `trading_service.py:773` | "cooldown set" listed as a write inside `_cover_internal`, but it's actually performed by `cover()` at line 700 after `_cover_internal` returns (still inside the same UoW envelope, so the rollback claim is correct). Slightly imprecise but not a regression — pre-fix docstring had the same phrasing. | Optional polish: rephrase to "every write the call site emits (target stub, user upsert, fund cash, price upsert, and the cooldown set `cover()` performs after this helper returns)" — not blocking. |
| L | `liquidation_service.py:65` | Class docstring is a single line ("Auto-covers short positions that have rallied past the liquidation threshold."). The module-level docstring is detailed but the class docstring could note the new UoW seam for symmetry with `TradingService` (which surfaces the UoW seam in its module docstring §"UoW envelope responsibility" but not in the class docstring either, so this is purely a "could mirror" note). | Optional: add a one-line "Holds a UoW envelope around `cover_forced` so liquidation writes commit atomically (issue #95)." — non-blocking. |

**VERDICT: CLEAN.** Gate green; 0 CRITICAL / 0 HIGH; all three ACs
verified with RED evidence and a mutation check confirming the test is
load-bearing; no new dependencies; docstring replacements are accurate.

## Next steps

1. Manager commits the existing worktree state to `fix/liquidation-uow`
   and opens a PR with `Closes #95` in the body.
2. PR template `Verification` block: gate output is captured at
   `baton-runner/issue-95-uow/gate-phase-1-iter-1/` and was rerun by
   this reviewer with the same PASS result.
3. (Optional, non-blocking) Pick up the MEDIUM cosmetic on the
   `_cover_internal` docstring in a follow-up sweep if a TradingService
   docstring pass is ever opened.

## Open questions / risks

- None. The work-unit is mergeable as-is.

## References

- Issues: [#95](https://github.com/z0rd0n88/Friendex/issues/95)
- Gate: `baton-runner/issue-95-uow/gate-phase-1-iter-1/` (PASS this run)
- Code:
  - `src/friendex/application/liquidation_service.py:78` (ctor kwarg)
  - `src/friendex/application/liquidation_service.py:186` (UoW wrap)
  - `src/friendex/application/trading_service.py:711-717,769-776` (AC3 docstrings)
  - `src/friendex/adapters/container.py:467` (wiring)
  - `tests/application/test_liquidation_service.py:451` (regression test)
- Previous batons: [000](./000-2026-06-05-uow-wrap-liquidation-kickoff.md),
  [001](./001-2026-06-05-red-test-captured.md),
  [002](./002-2026-06-05-uow-fix-complete.md)
