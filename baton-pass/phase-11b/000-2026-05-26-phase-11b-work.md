# Pass-Baton: phase-11b read-only cogs (portfolio + stats) — in progress

**Date:** 2026-05-26
**Scope:** phase-11b
**Branch:** feat/phase-11b-cogs-read
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-26-phase-11
**HEAD:** 65a8902 chore(phase-11a): review CLEAN — digest + state + log

## Where things stand

Work-unit kickoff. Phase 11a is merged into the worktree (cogs foundation
+ AccountCog/DailyCog/AdminCog + conftest fixtures) and is **the baseline
this work stacks on**. Phase 11b's scope is the two read-only cogs from the
Phase 11 spec:

1. `portfolio_cog.py` — `/portfolio [user]` (EPHEMERAL; default user = invoker).
2. `stats_cog.py` — `/trending` (PUBLIC), `/mystats` (EPHEMERAL), `/price <user>` (EPHEMERAL), `/mystock` (EPHEMERAL).

Baseline `pytest tests/adapters/discord_bot/cogs/` → 33 passing.
Phase 11a conftest already exposes `portfolio_service`, `stats_service`, and
both service factories — to be reused. `_interaction.guild_id_of` is the
guild-id helper to import.

Confirmed read of:
- `baton-runner/br-2026-05-26-phase-11/digest-phase-11a.md` (conventions).
- `baton-runner/br-2026-05-25-phase-8/digest-phase-8d.md` (service contracts).
- `src/friendex/adapters/discord_bot/cogs/{_interaction,account_cog,daily_cog}.py`.
- `tests/adapters/discord_bot/cogs/{conftest,test_account_cog,test_daily_cog}.py`.
- `src/friendex/application/{portfolio_service,stats_service,snapshot_models}.py`.
- `src/friendex/adapters/discord_bot/embeds.py` (`build_portfolio_embed`,
  `build_trending_embed`, `build_mystats_embed`, `build_price_embed`).

TDD discipline: applying the same approach Phase 11a used (no `tdd` slash
command is registered; mirroring its RED→GREEN cadence on the new file
each iteration). Will capture RED output verbatim per AC before each impl
step.

## Next steps

1. Write RED test file `tests/adapters/discord_bot/cogs/test_portfolio_cog.py`,
   confirm import error, capture RED.
2. Implement `src/friendex/adapters/discord_bot/cogs/portfolio_cog.py` with
   `/portfolio [user]` callback; default user = invoker; explicit user
   override; per-guild factory routing; `None`-snapshot fallback (mirroring
   `AccountCog.balance`).
3. Repeat RED→GREEN for `stats_cog.py` covering `/trending`, `/mystats`,
   `/price`, `/mystock`.
4. Mutation-hardening tests: ephemeral flag flip; default-self drop.
5. Gate: ruff check, ruff format --check, mypy src/friendex, pytest.

## Open questions / risks

- `/portfolio` `snapshot is None` fallback prose. Mirroring `AccountCog.balance`
  decision (small inline `COLOR_NEUTRAL` embed with prose pointing at `/daily`
  to create an account). Documented if deviated.
- `/price <user>` returning `None` (no price history): plan to reply
  ephemerally with a small inline `COLOR_NEUTRAL` embed saying the target has
  no price data yet. This matches the read-only / brand-new-account
  convention from Phase 11a.

## References

- Phase 11a digest: `baton-runner/br-2026-05-26-phase-11/digest-phase-11a.md`
- Phase 8d digest: `baton-runner/br-2026-05-25-phase-8/digest-phase-8d.md`
- Phase 10 embeds digest: `baton-runner/br-2026-05-26-phase-10/digest-phase-10.md`
- Embed builders: `src/friendex/adapters/discord_bot/embeds.py:194,388,433,460`
- Service surface: `src/friendex/application/portfolio_service.py:135` (`portfolio_snapshot`)
- Service surface: `src/friendex/application/stats_service.py:73,108,133` (trending / user_stats / get_price_stats)
- Issue: #2 (phase status)
