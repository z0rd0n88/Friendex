# Pass-Baton: Phase 13 implementation in flight

**Date:** 2026-05-27
**Scope:** phase-13
**Branch:** feat/phase-13-container
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-13
**HEAD:** 85bb0fc feat(phase-12b): voice + message listeners + CF-1/CF-2/CF-4 fixes (#62)

## Where things stand

Phase 13 (Error Handler + Container Wiring) under implementation in the
worktree above. STATE.md acceptance criteria AC1–AC6 are the bar.

Plan: TDD per AC — write the test, run it RED, capture the failing output,
implement, run GREEN. RED output captured below per AC as we go.

## RED captures (TDD evidence, per-AC)

### AC4 (error handler tests) + AC1 (error handler impl)

RED capture before any `error_handler.py` source existed:

```
tests/adapters/discord_bot/test_error_handler.py:41: in <module>
    from friendex.adapters.discord_bot.error_handler import register_error_handler
E   ModuleNotFoundError: No module named 'friendex.adapters.discord_bot.error_handler'
```

GREEN: `7 passed` after writing `src/friendex/adapters/discord_bot/error_handler.py`.

(Mid-impl, a second RED surfaced for the CRITICAL log — `rec.exc_info[0]` was
`None` because `exc_info=True` reads `sys.exc_info()` which is empty when the
handler is dispatched outside an ``except:`` frame. Fixed by passing the
explicit `(type, value, tb)` tuple constructed from the unwrapped exception —
preserves the "log full traceback at CRITICAL" contract under both live
exception context and ad-hoc dispatch.)


## Next steps

1. AC4 — write `tests/adapters/discord_bot/test_error_handler.py` and run RED
   before any `error_handler.py` source exists.
2. AC1 — implement `src/friendex/adapters/discord_bot/error_handler.py` until
   green.
3. AC5 — write `tests/adapters/test_container.py` and run RED before
   container.py exists.
4. AC2 — implement `src/friendex/adapters/container.py`.
5. AC3 — implement `src/friendex/main.py` (raises NotImplementedError at the
   bot-construction marker for Phase 14).
6. AC6 — re-export `main` from `src/friendex/__init__.py`.
7. Run gate: pytest (subset + full), ruff check, ruff format --check, mypy.

## Inventory (discovered, locked)

- Repos (6): `SqlUserRepository`, `SqlFundRepository`, `SqlPriceRepository`,
  `SqlTradeCooldownRepository`, `SqlPenaltyRepository`,
  `SqlSystemStateRepository` — all take `sessionmaker: async_sessionmaker[AsyncSession]`.
- Services taking `guild_id` (per-guild factories): ActivityService,
  VoicePingService, PriceTickService, TradingService, PortfolioService,
  StatsService, FundService, DailyService, LiquidationService,
  DisciplineService.
- VoiceSessionStore + VoicePingSessionStore are per-guild stateful in-memory
  stores. Phase 12b ctor wants `voice_session_store_factory: Callable[[str], VoiceSessionStore]`
  — so Container must own a `dict[str, VoiceSessionStore]` and lazily build.
  Same for `VoicePingSessionStore` (consumed by ActivityService and VoicePingService).
- Cogs (7): AccountCog, AdminCog, DailyCog, FundCog, PortfolioCog, StatsCog,
  TradingCog (FundCog wires `.group: FundGroup`).
- Listeners (4): MessageListener, VoiceListener, ReactionListener, MemberListener.
- Tasks (8): ActivityTickTask, DailyResetTask, FreezeCheckTask,
  InactivityDecayTask, LiquidationTask, MonthlyRolloverTask, VcBoostTask,
  WeeklyResetTask.
- LiquidationService also requires a `trading_service: TradingService` — the
  factory builds Trading first, then Liquidation closing over it.
- LiquidationTask needs a `notifier: Callable[[LiquidationEvent], Awaitable[None]]`.
  Phase 14 wires the real notifier; Phase 13 uses a stub no-op notifier (logs only)
  so the task is constructable but not started.
- ActivityTickTask binds to `PriceTickService.activity_price_tick()` per Phase 9 digest.

## Open questions / risks

- `iter_guild_ids` for tasks must be provided. Phase 14 wires it to
  `discord.Client.guilds`. Phase 13: define an `async def _iter_no_guilds() -> []`
  placeholder on the Container (no start, no fire). Document in baton.
- LiquidationTask notifier: stub `async def _noop_notifier(event)` in container
  (defence in depth — task never runs in Phase 13).

## References

- Spec: `docs/04-migration-plan.md` §Phase 13 (lines 735-761)
- STATE: `baton-runner/br-2026-05-27-phase-13/STATE.md`
- Continuity digests in `baton-runner/br-2026-05-*/digest-*.md` (phases 6–12)
