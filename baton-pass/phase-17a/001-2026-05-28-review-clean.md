# Pass-Baton: Phase 17a review — CLEAN

**Date:** 2026-05-28
**Scope:** phase-17a
**Branch:** feat/phase-17a-toggles
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-28-phase-17
**HEAD:** 5c294fe feat(phase-17a): config toggles for Sunday-buy, APY period, opt-out

## Verdict

**VERDICT: CLEAN.** Deterministic gate is green, all four acceptance criteria
(A1–A5) are genuinely met, every mutation probe (M1–M4) failed the matching
test under revert (then reverted), dependency invariant holds, and the
change-set is strictly inside the allow-list.

## Gate result

`bash scripts/gate.sh baton-runner/br-2026-05-28-phase-17/gate-phase-17a-iter-1/`:

```
=== gate: pytest ($*: uv run pytest) ===
PASS pytest
=== gate: ruff-check ($*: uv run ruff check src tests alembic) ===
PASS ruff-check
=== gate: ruff-format ($*: uv run ruff format --check src tests alembic) ===
PASS ruff-format
=== gate: mypy ($*: uv run mypy src/friendex) ===
PASS mypy
----
GATE: PASS
```

## Acceptance criteria verification

| AC | Status | Evidence |
|---|---|---|
| A1 — three new Settings fields with exact defaults + env override tests | met | `src/friendex/adapters/config.py:81,87,168`. Defaults pinned in `tests/adapters/test_config.py:278-287`. Env overrides + invalid-Literal rejection in `tests/adapters/test_config.py:434-481`. `.env.example:33-50`. |
| A2 — `/buy` reads `settings.sunday_buy_allowed`; sell/short/cover stay hard-coded `False` | met | `src/friendex/application/trading_service.py:341`. Sell/short/cover unchanged. RED test `test_buy_rejected_on_sunday_when_sunday_buy_allowed_is_false` at `tests/application/test_trading_service.py:1513-1540` freezes 2026-05-24 12:00 UTC (Sunday — verified). |
| A3 — `_check_opt_in` early-returns when toggle False; raises at default | met | `src/friendex/application/trading_service.py:311-322`. Tests at `tests/application/test_trading_service.py:1543-1587`. All four callers (`trading_service.py:347,425,498,652`) are `self._check_opt_in(target)` so the `@staticmethod`→instance change is source-compatible. |
| A4 — `accrue_apy` passes `period=settings.hedge_fund_base_apy_period` | met | `src/friendex/application/fund_service.py:339-343`. Annual ($15.00) and default-monthly ($1.25) RED tests at `tests/application/test_fund_service.py:611-671`. |
| A5 — gate green, zero pyproject/uv.lock diff, allow-list only | met | `git diff origin/main -- pyproject.toml uv.lock` → 0 bytes. `git diff --name-only origin/main...HEAD -- src/ tests/ .env.example` lists exactly the seven allow-listed files. |

## Mutation probes (M1–M4)

Each probe was applied in-place, the matching test run, the failure line
captured, and the change reverted. After all four reverts `git diff src/
tests/ .env.example` is empty (verified).

- **M1** revert `allow_sunday=self._settings.sunday_buy_allowed` → literal `True` at
  `src/friendex/application/trading_service.py:341`.
  - `test_buy_rejected_on_sunday_when_sunday_buy_allowed_is_false` failed:
    `tests/application/test_trading_service.py:1539: Failed: DID NOT RAISE
    <class 'friendex.domain.errors.MarketClosed'>`.
- **M2** delete the toggle early-return inside `_check_opt_in`
  (`src/friendex/application/trading_service.py:319-320`).
  - `test_check_opt_in_is_noop_when_opt_out_blocks_trading_is_false` failed:
    `src/friendex/application/trading_service.py:320: OptedOut: <@target-1>
    has opted out of trading.`
- **M3** hard-code `period="monthly"` in `accrue_apy` at
  `src/friendex/application/fund_service.py:339-343`.
  - `test_accrue_apy_uses_annual_period_when_setting_is_annual` failed:
    `tests/application/test_fund_service.py:643: AssertionError: assert
    Decimal('101.25') == Decimal('115.00')`.
- **M4** change `opt_out_blocks_trading: bool = True` → `False` at
  `src/friendex/adapters/config.py:87`.
  - `test_defaults_match_target_architecture` failed:
    `tests/adapters/test_config.py:286: AssertionError: assert False is True`.

All four reverts confirmed in place; `git status` shows zero product-code
modifications post-review.

## Continuity invariants

- Decimal-at-boundary preserved: `accrue_apy` still operates on
  `Decimal` cash balances; no float arithmetic was introduced.
- UTC-aware datetimes preserved: Sunday test uses `datetime(2026,5,24,12,0,tzinfo=UTC)`.
- pydantic-settings v2 conventions honoured — `Literal["monthly","annual"]`
  follows the existing `log_format: Literal["json","console"]` pattern at
  `src/friendex/adapters/config.py:174`.
- No `discord` import in `domain/`, `application/`, `persistence/` (re-checked).
- Cogs/listeners untouched — `DomainError` still propagates at default toggle.
- Composite lock keys + LockManager unchanged.
- Phase 16 smoke-driver / `STEPS[id=18]` / `test_fund_invest_step_notes_not_implemented_error`
  untouched — those remain Phase 17c's territory.
- Existing Phase-8c `test_buy_allowed_on_sunday`, Phase-8e
  `test_e6_accrue_apy_credits_monthly_amount_to_each_personal_fund`, and
  C6 opt-out tests all still green at defaults (re-ran post-revert: 6 passed).

## Dependency surface

`git diff origin/main -- pyproject.toml uv.lock` → 0 bytes. No new runtime
dependency introduced.

## Findings

- **CRITICAL:** none.
- **HIGH:** none.
- **MEDIUM:** none.
- **LOW:** none.
- **INFO 1:** Settings-field ordering note. `sunday_buy_allowed` and
  `opt_out_blocks_trading` are placed under the "Market hours" comment block
  (`src/friendex/adapters/config.py:72-87`) even though
  `opt_out_blocks_trading` is conceptually an opt-in/trading toggle rather
  than a market-hours setting. Not a defect — operator-facing UX only — but
  a future grouping refactor could split them into a dedicated "Trading
  toggles" section to match `.env.example`'s grouping. No action required.
- **INFO 2:** Test `test_check_opt_in_is_noop_when_opt_out_blocks_trading_is_false`
  calls `service._check_opt_in(...)` directly (a private method). That is
  unavoidable for a unit-level verification of the toggle behaviour and
  consistent with how the implementer chose to expose the seam. No fix.

## Phase 17b/17c handoff (digest companion)

The phase-exit digest at
`baton-runner/br-2026-05-28-phase-17/digest-phase-17a.md` records the public
surface and decisions Phase 17b/17c must honour (Settings fields, accrue_apy
period threading, `/buy` Sunday check, `_check_opt_in` early-return semantics).

## References

- Implementer baton: `baton-pass/phase-17a/000-2026-05-28-toggles-implemented.md`
- Spec: `docs/04-migration-plan.md` §Phase 17 lines 858-890
- Open-Qs: `docs/02-target-architecture.md` §Open-Questions Q2 / Q3 / Q8
- Code:
  - `src/friendex/adapters/config.py:76-87,161-168` (Settings toggle fields)
  - `src/friendex/application/trading_service.py:311-322` (`_check_opt_in`)
  - `src/friendex/application/trading_service.py:341` (`buy` Sunday wiring)
  - `src/friendex/application/fund_service.py:339-343` (`accrue_apy` period wiring)
- Tests:
  - `tests/adapters/test_config.py:278-287,434-481`
  - `tests/application/test_trading_service.py:1513-1587`
  - `tests/application/test_fund_service.py:611-671`
- Gate logs: `baton-runner/br-2026-05-28-phase-17/gate-phase-17a-iter-1/`
- Issue: #2 (phase status)
