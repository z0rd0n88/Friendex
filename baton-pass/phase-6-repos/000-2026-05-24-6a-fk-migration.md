# Pass-Baton: Sub-unit 6a — FK enforcement + 0002 cascade migration

**Date:** 2026-05-24
**Scope:** phase-6-repos
**Branch:** feat/phase-6-repos
**Worktree:** /home/alex/Friendex/.claude/worktrees/phase-6-repos
**HEAD:** f7aa6f1 chore(baton-runner): set up phase-6 run + preserve phase-5 FK baton

## Where things stand

Implementing sub-unit **6a-fk-migration** (first of 6 sub-units of Phase 6). Four
acceptance criteria, each TDD'd RED→GREEN. Status:

- [x] **AC1 — PRAGMA foreign_keys=ON listener** in `db.py`. RED captured
  (`assert 0 == 1`), GREEN. Listener `_enable_sqlite_foreign_keys` attached
  inside `build_engine` via `@event.listens_for(engine.sync_engine, "connect")`,
  guarded to the `sqlite` dialect. Test: `tests/adapters/persistence/test_db.py`.
- [ ] **AC2 — Alembic 0002 ON DELETE CASCADE migration** + `render_as_batch=True`
  on both `context.configure` calls in `alembic/env.py`. Pending.
- [ ] **AC3 — Decimal quantisation assertions** (MEDIUM carry-forward) in
  `test_orm.py` — `.as_tuple().exponent` per DecimalText column + a
  float-inexact fixture value. Pending.
- [ ] **AC4 — real `compare_metadata` drift test** (LOW carry-forward) in
  `test_migrations.py`. Pending.

## Next steps

1. AC2: add `render_as_batch=True` to both `context.configure(...)` in
   `alembic/env.py`; author `alembic/versions/0002_*.py` using
   `op.batch_alter_table` to add `ON DELETE CASCADE` to child FKs (long/short
   positions → users; activity_buckets → users; voice_unique_channels →
   activity_buckets; price_history → stocks; fund_investors → hedge_funds).
   Note: `fund_penalties` has NO FK in orm.py (keyed by guild_id,user_id but no
   ForeignKeyConstraint) — confirm scope before adding. Reversible up+down.
   Test in `test_migrations.py`: head→base→head clean; parent delete cascades.
2. AC3 + AC4 as above.
3. Run `scripts/gate.sh baton-runner/br-2026-05-24-phase-6/selfcheck-6a/` → must
   print `GATE: PASS`.

## Open questions / risks

- `fund_penalties` and `system_state`/`trade_cooldowns` have no FK constraints in
  orm.py — they are NOT child tables, so 0002 leaves them untouched. The child
  FKs needing CASCADE are the 5 `ForeignKeyConstraint(..., ondelete="CASCADE")`
  already declared in orm.py (so `create_all` already builds them with CASCADE;
  0002 only matters for an existing DB upgraded from the 0001 baseline, but since
  0001 also runs `create_all` the CASCADE is already present — 0002 must still be
  authored to satisfy the acceptance criterion and prove batch-mode reversibility).

## References

- Spec: `docs/04-migration-plan.md` §"Phase 6 — Persistence" (L345-388)
- ADR: `docs/adr/0002-sqlite-fk-enforcement.md`
- Code: `src/friendex/adapters/persistence/db.py` (listener); `alembic/env.py`
- Tests: `tests/adapters/persistence/test_db.py`, `test_orm.py`, `test_migrations.py`
