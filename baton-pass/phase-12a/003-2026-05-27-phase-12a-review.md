# Pass-Baton: Phase 12a independent review (iter-1 retry) — VERDICT CLEAN

**Date:** 2026-05-27
**Scope:** phase-12a
**Branch:** feat/phase-12a-listeners-simple
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-12
**HEAD:** d43fe2a chore(phase-12a): record invalidated review iter-1 + manager revert

## Where things stand

**VERDICT: CLEAN.** Phase 12a (foundation listeners — reaction + member) is
ready to merge. The prior review (`002-2026-05-27-phase-12a-review.md`)
returned ISSUES on a false-positive CRITICAL claiming
`member_listener.py:65` used `"ban"` instead of `"timeout"`; that reviewer
applied mutation M3 (flip `"timeout"` ↔ `"ban"`) and failed to revert it
before reading the file, then misread its own injected mutation as a
product bug. HEAD has `"timeout"` on line 65, matching A5. This baton
supersedes 002 — do not re-cite the 002 CRITICAL.

This review was conducted on a clean tree (`git status` empty, no product
diff) confirmed before starting. All mutations applied here were reverted
immediately; `git diff` returns empty against product files now.

## Gate result (clean tree)

`scripts/gate.sh baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-1-retry/`
→ `GATE: PASS`

- `pytest`: PASS — 724 tests pass.
- `ruff check src tests alembic`: PASS.
- `ruff format --check src tests alembic`: PASS.
- `mypy src/friendex`: PASS.
- Coverage on new listener surface: **100%** on
  `member_listener.py` (18 stmts, 4 branches), `reaction_listener.py`
  (17 stmts, 6 branches), `listeners/__init__.py` (empty). A8's ≥80% bar
  is easily cleared.

## Acceptance Criteria — A1–A8 all met

- **A1:** `src/friendex/adapters/discord_bot/listeners/__init__.py` exists, 0
  bytes — matches `cogs/__init__.py` convention (also 0 bytes).
- **A2:** `tests/adapters/discord_bot/listeners/__init__.py` exists with a
  docstring describing the test-package idiom.
- **A3:** `conftest.py` exposes `fake_message`, `fake_member`,
  `fake_voice_state` factories with the documented signatures, plus
  `AsyncMock(spec=Service)` fixtures and matching `<svc>_service_factory`
  fixtures for **all** Phase-12 services (activity, discipline, voice_ping,
  trading, fund, daily, portfolio, stats). 12b inherits this conftest
  unchanged.
- **A4:** `ReactionListener(commands.Cog).on_reaction_add` skips
  `user.bot`, self-reactions, and DMs (`guild is None`); delegates via
  `activity_service_factory(str(guild.id))`; ctor takes a per-guild
  factory.
- **A5:** `MemberListener.on_member_update` fires
  `apply_discipline_penalty(str(after.id), "timeout")` **only** on
  `before.timed_out_until is None and after.timed_out_until is not None`
  (verified at `member_listener.py:59-65`). `on_member_ban` fires
  `"ban"` for every ban (line 75). Ctor takes the discipline factory.
- **A6:** Mutation-hardened tests exist and pin the three required mutations
  (see "Mutation results" below).
- **A7:** No `try/except` anywhere in either listener. Propagation tests:
  `test_on_reaction_add_propagates_domain_error` (`OptedOut`),
  `test_on_member_update_propagates_domain_error`,
  `test_on_member_ban_propagates_domain_error` (both `DomainError`).
- **A8:** Gate green, 100% coverage on the new files.

## Mutation results (each reverted before next step)

| ID | Mutation | Pinned test(s) — actual RED output |
|---|---|---|
| M1 | Drop `if user.bot: return` in `reaction_listener.py` | `test_on_reaction_add_skips_bot_reactor` FAILED — `AssertionError: Expected 'record_reaction' to not have been called. Called 1 times. Calls: [call(user_id='42')].` |
| M2 | Drop `if before.timed_out_until is not None: return` in `member_listener.py` | `test_on_member_update_does_not_fire_on_extension` FAILED — `AssertionError: Expected 'apply_discipline_penalty' to not have been called. Called 1 times. Calls: [call('42', 'timeout')].` (the other two no-op tests still pass because the `after is None` guard covers un-timeout / none-to-none; the extension case is the load-bearing pin for the `before is None` guard) |
| M3 | Flip `"timeout"` ↔ `"ban"` at lines 65 and 75 | 4 tests FAILED: `test_on_member_update_passes_kind_timeout_literal`, `test_on_member_update_fires_timeout_on_none_to_set`, `test_on_member_ban_passes_kind_ban_literal`, `test_on_member_ban_fires_ban_penalty` — all with explicit `'ban' != 'timeout'` style assertions. |

After each mutation, `git checkout HEAD -- <file>` then `git diff` returned
empty before moving on. Final `git status` on product code: clean.

## Continuity sweep

- **Phase 11a §1/§3/§5/§7** (factory ctor, no module-level constants,
  DomainError propagation, mutation-hardening): all four honoured.
- **Phase 10** (no-discord-leak): only `listeners/` imports `discord`;
  domain/application/persistence/tasks remain clean. Listeners are the
  third allowed discord-importing layer per STATE.md.
- **Phase 8f** (kind contract): `"timeout"` and `"ban"` are the canonical
  literals passed to `DisciplineService.apply_discipline_penalty`.
- **No new dependencies** — `git diff ea4b7b2..HEAD -- pyproject.toml uv.lock`
  is empty.

## Findings

None at any severity. No CRITICAL, HIGH, MEDIUM, or LOW findings. The work
is idiomatic, test-hardened, and faithful to every Phase-12 signoff
decision.

## Next steps

1. Manager: mark phase-12a unit COMPLETE in STATE.md; promote `phase-12a`
   to PR-ready and open the stacked PR against `origin/main@ea4b7b2`.
2. Phase 12b kicks off on `feat/phase-12b-listeners-voice` (base
   `feat/phase-12a-listeners-simple`). The 12a `conftest.py` already
   exposes the voice/message service fixtures 12b needs — no conftest
   churn expected.
3. Phase exit digest written at
   `baton-runner/br-2026-05-27-phase-12/digest-phase-12a.md` for 12b's
   continuity.

## References

- Issues: #2 (Phase status tracker)
- Spec: `docs/04-migration-plan.md` §Phase 12 (lines 702-732)
- Run state: `baton-runner/br-2026-05-27-phase-12/STATE.md`
- Gate logs: `baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-1-retry/`
- Code: `src/friendex/adapters/discord_bot/listeners/reaction_listener.py`,
  `src/friendex/adapters/discord_bot/listeners/member_listener.py`
- Tests: `tests/adapters/discord_bot/listeners/`
- Prior batons in scope:
  - `baton-pass/phase-12a/000-2026-05-27-phase-12a-kickoff.md`
  - `baton-pass/phase-12a/001-2026-05-27-phase-12a-complete.md`
  - `baton-pass/phase-12a/002-2026-05-27-phase-12a-review.md` (SUPERSEDED — false-positive CRITICAL from un-reverted mutation; do not cite)
