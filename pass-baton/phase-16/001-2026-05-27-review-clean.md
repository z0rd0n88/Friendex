# Pass-Baton: Phase 16 review — CLEAN

**Date:** 2026-05-27
**Scope:** phase-16
**Branch:** feat/phase-16-cutover
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-16
**HEAD:** 5b8da42 chore: smoke test driver script

## Where things stand

Review of the Phase 16 cutover artefacts (`scripts/smoke_test_commands.py`,
`tests/scripts/test_smoke_test_commands.py`, `docs/runbook-smoke-test.md`,
plus two new `__init__.py` shims) is **CLEAN**. Gate is green, the
two-commit split (`docs:` then `chore:`) matches the spec, AC1-AC6 are
all met with real (non-tautological) tests, and every prior digest's
carry-forward is honoured. Currently blocking: **nothing — ready for
PR.**

## Gate evidence

`bash scripts/gate.sh /home/user/Friendex/baton-runner/br-2026-05-27-phase-16/gate-phase-16-iter-1/`
final run:

```
PASS pytest           (803 passed, 1 warning in ~13s)
PASS ruff-check       (src tests alembic)
PASS ruff-format      (src tests alembic)
PASS mypy             (src/friendex — 70 files, no issues)
GATE: PASS
```

Initial gate run was FAIL because the worktree had an **unstaged**
mutation on `scripts/smoke_test_commands.py` (STEPS converted to a list
with `# type: ignore[assignment]`) — a leftover from a prior review
iteration. It was not part of either commit. I stashed and then dropped
the stash; the working tree is now back to `HEAD` clean. The two
committed files are the canonical Phase 16 artefacts.

## Mutation menu — all six FAIL the right test

| # | Mutation | Test that FAILed | Evidence (short) |
|---|---|---|---|
| M1 | Drop `/balance` SmokeStep | `test_every_slash_command_is_represented_exactly_once` | "Extra items in the right set: '/balance'" |
| M2 | Drop `on_voice_state_update` SmokeStep | `test_every_listener_event_is_present` | "listener event 'on_voice_state_update' is not represented in STEPS" |
| M3 | Drop `15-min activity tick` SmokeStep | `test_every_background_task_is_present` | "background task 'activity tick' is not represented in STEPS" |
| M4 | Reverse print order (`key=lambda s: -s.id`) | `test_main_prints_steps_in_strict_id_order` | "assert [3, 2, 1, 20, 19, 18, ...] == [1, 2, 3, ...]" |
| M5 | Convert `STEPS` tuple → list | `test_steps_is_a_tuple_not_a_list` + `test_steps_tuple_rejects_append` + `test_steps_tuple_rejects_item_assignment` (3 tests) | "DID NOT RAISE AttributeError" / "DID NOT RAISE TypeError" |
| M6 | Drop "NotImplementedError" from `/fund invest` expected text | `test_fund_invest_step_notes_not_implemented_error` | "assert 'notimplementederror' in 'invoke /fund invest …'" |

All six mutations reverted; gate re-ran green after final revert; working
tree clean.

## Deeper invariants — verified

- `STEPS: Final[tuple[SmokeStep, ...]]` AND `@dataclass(frozen=True, slots=True)`
  — both immutability layers present (script:39, 66).
- Determinism: `uv run python scripts/smoke_test_commands.py` twice →
  `md5sum` `2bce67e1d4993e0f1242d8ee3a236aa4` both runs; `diff` empty.
- Runbook does NOT enumerate the step list. `grep -nE '/balance|/daily|…'`
  on `docs/runbook-smoke-test.md` returns only post-flight prose lines
  133 and 137 (`/buy`, `/daily` as example DomainError paths). No
  duplicate checklist.
- Migrator pre-flight (runbook §2) matches Phase 15b digest verbatim:
  `--guild-id <id> --dry-run --report`, alphabetical report ordering
  called out, `--target` not opened by `--dry-run`, orphan check
  warn-not-fail.
- No `discord` import in `scripts/` or `tests/scripts/`
  (`grep -rn '^import discord\|^from discord' …` empty).
- No edits under `src/friendex/`; no `pyproject.toml` / `uv.lock` change.
- Two-commit split: `HEAD~1 a2c5af5 docs: smoke test runbook` (167 lines
  in `docs/runbook-smoke-test.md` only); `HEAD 5b8da42 chore: smoke test
  driver script` (script + tests + two `__init__.py` shims). Order +
  scope match `docs/04-migration-plan.md` §Phase 16.

## Findings

- **CRITICAL:** none.
- **HIGH:** none.
- **MEDIUM:** none.
- **LOW:** none. Considered but rejected:
  - Runbook sign-off table cites step id *ranges* (1-3, 4-20, 21-25,
    26-31, 32). These are section bucket markers, not a duplicated step
    list, so the single-source-of-truth contract is intact. If Phase 17
    adds steps and the ranges drift, the test suite won't catch it —
    acceptable risk; the bucket ranges are a sign-off-table affordance,
    not the checklist.

## Verdict

**CLEAN** — ready for PR.

## References

- Implementation baton: `pass-baton/phase-16/000-2026-05-27-smoke-test-driver-implemented.md`
- Spec: `docs/04-migration-plan.md` §Phase 16 (lines 828-854)
- Phase tracker: GitHub issue #2
- Digests honoured: `baton-runner/br-2026-05-27-phase-15/digest-phase-15b.md`,
  `baton-runner/br-2026-05-27-phase-14/digest-phase-14.md`,
  `baton-runner/br-2026-05-27-phase-13/digest-phase-13.md`,
  `baton-runner/br-2026-05-27-phase-12/digest-phase-12a.md`,
  `baton-runner/br-2026-05-27-phase-12/digest-phase-12b.md`,
  `baton-runner/br-2026-05-26-phase-11/digest-phase-11c.md`,
  `baton-runner/br-2026-05-25-phase-9/digest-phase-9.md`
- Code under review: `scripts/smoke_test_commands.py:1`,
  `tests/scripts/test_smoke_test_commands.py:1`,
  `docs/runbook-smoke-test.md:1`
