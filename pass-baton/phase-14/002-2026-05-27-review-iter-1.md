# Pass-Baton: Phase 14 review iter-1 — VERDICT CLEAN

**Date:** 2026-05-27
**Scope:** phase-14
**Reviewer:** python-pro (review unit)
**Branch:** feat/phase-14-bot-factory
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-14
**HEAD:** 0d4fbf1 feat(phase-14): bot factory + entry point wiring (base 4c591f0)

## Gate

`bash scripts/gate.sh baton-runner/br-2026-05-27-phase-14/gate-phase-14-iter-1/`
- PASS pytest (786 passed, 1 warning, 8.30s)
- PASS ruff-check
- PASS ruff-format
- PASS mypy (0 errors / 70 source files)
- GATE: PASS

## Findings

**CRITICAL:** none.
**HIGH:** none.
**MEDIUM:** none.
**LOW:** none new (the caplog/alembic carry-forward LOW from the implementer baton is acknowledged and documented inline at tests/adapters/test_container.py:378 and :422; a one-line fix in alembic/env.py would clear it but is properly deferred — not a Phase-14 regression).

## Acceptance-criteria verification

- **AC1 — build_bot** (src/friendex/adapters/discord_bot/bot.py): Uses `command_prefix=commands.when_mentioned` (passed unbound — correct discord.py idiom for slash-only bots) and `intents=discord.Intents.all()` (bot.py:78-81). `setup_hook` performs the exact ordering required: register_with → bind_runtime → task.start ×8 → global tree.sync → conditional dev-guild copy_global_to + sync (bot.py:102-110). Attribute-assign style (`bot.setup_hook = setup_hook`) mirrors Phase-13's `bot.tree.on_error =` pattern with the single sanctioned method-assign type-ignore (bot.py:116). Tests pin all of this (test_bot_factory.py:47-99) and the dev-guild branch (test_bot_factory.py:102-125).

- **AC2 — bind_runtime** (src/friendex/adapters/container.py:460-497): Iterates `self.tasks` (8 entries) and calls `setattr(task, "_iter_guild_ids", iter_guild_ids)` on every one (container.py:495-496); the closure re-reads `bot.guilds` on every tick (container.py:484-487, pinned by test_bind_runtime_iter_guild_ids_reflects_live_bot_guilds). Notifier replacement (container.py:497) uses `_make_liquidation_notifier(bot)` which (a) sends with `AllowedMentions.none()` (container.py:164), (b) WARN-and-skips on missing guild (container.py:149-154) and missing system_channel (container.py:156-161). All three branches are pinned by dedicated tests (test_container.py:299-428). `_liquidation_task` stored reference avoids the isinstance scan called out in STATE.md.

- **AC3 — main.py** (src/friendex/main.py): `NotImplementedError` seam removed; replaced with `bot = build_bot(settings, container); await bot.start(settings.discord_token)` (main.py:51-52). `engine.dispose()` preserved in `finally` (main.py:53-54). The Phase-13 `Settings()  # type: ignore[call-arg]` is preserved (main.py:44) — no new unjustified ignores in this module.

- **AC4 — bot factory tests** (tests/adapters/discord_bot/test_bot_factory.py): 7 tests, all real assertions — would catch mutation (intents → Default would fail line 59; setup_hook → no-op would fail :71; missing task.start call → fails :94; missing dev-guild sync → fails :119-123; missing bind_runtime → fails :147-153; missing register_with → fails :181).

- **AC5 — integration flow** (tests/integration/test_full_command_flow.py): Two tests, both run on a real Container over an in-memory SQLite engine with the full schema. `test_build_bot_wires_real_container` proves the bot factory composes end-to-end with the real container. `test_full_command_flow_daily_buy_portfolio` exercises /daily → /buy → /portfolio with DB read-back assertions on cash_balance (10500 → 10400), long_positions (target_id present with 1 share), and ephemeral kwarg on /portfolio. The direct cog-callback fallback (STATE.md AC5 explicit permission) is correctly documented in the module docstring with the rationale why dpytest cannot drive app_commands cleanly.

- **AC6 — bind_runtime tests** (tests/adapters/test_container.py:249-428): 6 tests covering live walk, closure freshness (not a snapshot), notifier replacement, system_channel dispatch with AllowedMentions.none(), missing-guild WARN, missing-system-channel WARN. Each has substantive assertions (not vacuous): would catch a regression where bind_runtime forgot a task, the closure cached guilds, the notifier dropped `allowed_mentions`, or the WARN+skip branches were inverted.

## Phase-13 carry-forward — MUST-honour reflection

1. `NotImplementedError` seam filled (main.py:51-52, replaces `raise NotImplementedError("Phase 14: build_bot + bot.start")`). HONOURED.
2. Per-task `_iter_guild_ids` swap via `bind_runtime(bot)` — chose the post-construction mutation path (the other admissible option was widening `Container.__init__`). HONOURED.
3. LiquidationTask notifier real dispatcher with the fixed signature `Callable[[LiquidationEvent], Awaitable[None]]` (container.py:132-167). HONOURED.
4. `bot.tree.on_error` attribute-assign pattern preserved (error_handler.py unchanged at this HEAD). The new `bot.setup_hook` assignment follows the same idiom. HONOURED.
5. `AllowedMentions.none()` on every reply path — present on the notifier send (container.py:164). HONOURED.
6. Type-ignore budget: src/friendex now carries exactly 3 ignores (main.py:44 call-arg, error_handler.py:177 method-assign, bot.py:116 method-assign). Budget was 3 carried + at most 1 new = 4; actual usage is 3 (one new method-assign, one Phase-13 call-arg counted as carried, one Phase-13 method-assign counted as carried). Within budget. Tests carry the Phase-13 attr-defined ignore at test_container.py:168 unchanged. The implementer's note about avoiding a 5th via `setattr(task, "_iter_guild_ids", ...) # noqa: B010` is sound — `_iter_guild_ids` is a subclass-only attribute the abstract base does not declare.
7. `_make_liquidation_factory` still builds a fresh TradingService per call; LockManager keyed `f"{guild}:{user}"` still serialises `_cover_internal` correctly — unchanged from Phase 13.

## Spec-deviation review (docs/04-migration-plan.md §Phase 14 vs delivered)

- `settings.guild_id` → `settings.dev_guild_id` rename — already documented in STATE.md, semantics preserved (sync is "global + optional dev-guild instant").
- main.py modified (spec says "Files modified: none") — Phase 13 STATE.md already flagged the spec inaccuracy at its own signoff, so this is not a new deviation.
- bind_runtime + real notifier added to container.py — explicitly mandated by Phase-13 digest items 2 and 3.
- `LiquidationEvent.guild_id` field added — the one substantive scope deviation. Verified: (a) field is `str` (matches Phase-8 guild_id convention), (b) `LiquidationService` populates it from `self._guild_id` (liquidation_service.py:164), (c) three downstream consumers were updated minimally with explicit fixture values (test_embeds.py +2, test_liquidation_task.py +1 in the _event helper, test_liquidation_service.py +1 explicit pin), (d) the F4 service test gained `assert event.guild_id == GUILD` so a regression that dropped the populate line would be caught. The alternative (per-guild notifier wrapper) would have contradicted Phase-13 digest item 3's fixed notifier signature; this is the correct trade-off.

## Dependencies

`git diff 4c591f0...HEAD -- pyproject.toml uv.lock` → no churn. No new dependencies added.

## Files reviewed (diff against base 4c591f0)

- src/friendex/adapters/discord_bot/bot.py (+117 NEW)
- src/friendex/adapters/container.py (+105: bind_runtime + _make_liquidation_notifier + _liquidation_task ref)
- src/friendex/application/liquidation_events.py (+5: guild_id field + docstring update)
- src/friendex/application/liquidation_service.py (+1: guild_id=self._guild_id)
- src/friendex/main.py (-NotImplementedError, +build_bot + bot.start)
- tests/adapters/discord_bot/test_bot_factory.py (+181 NEW, 7 tests)
- tests/adapters/test_container.py (+198, 6 new bind_runtime tests — total 19)
- tests/adapters/discord_bot/test_embeds.py, tests/adapters/tasks/test_liquidation_task.py, tests/application/test_liquidation_service.py (+1 each: LiquidationEvent.guild_id wiring)
- tests/integration/__init__.py (NEW empty marker)
- tests/integration/test_full_command_flow.py (+276 NEW, 2 tests)

## Verdict

**VERDICT: CLEAN.** Gate is green, every AC is met with non-vacuous tests, all 7 Phase-13 MUST-honour items are honoured, type-ignore budget is within bounds (3/4 in src/), and the one scope deviation (`LiquidationEvent.guild_id`) is justified and properly threaded through all consumers with explicit test assertions. Proceeding to phase-exit digest.

## References

- Implementer baton: pass-baton/phase-14/001-2026-05-27-phase-14-complete.md
- STATE: baton-runner/br-2026-05-27-phase-14/STATE.md
- Phase-13 digest: baton-runner/br-2026-05-27-phase-13/digest-phase-13.md
- Phase-14 exit digest: baton-runner/br-2026-05-27-phase-14/digest-phase-14.md
- Gate logs: baton-runner/br-2026-05-27-phase-14/gate-phase-14-iter-1/{pytest,mypy,ruff-check,ruff-format}.log
