# Pass-Baton: Phase 14 — Bot Factory & Entry Point (work unit start)

**Date:** 2026-05-27
**Scope:** phase-14
**Branch:** feat/phase-14-bot-factory
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-14
**HEAD:** 4c591f0 feat(phase-13): error handler + container wiring (#64)

## Where things stand

Phase 13 shipped: `Container` builds 6 repos, 10 service factories, 8 tasks
(not started), 7 cogs, 4 listeners; `register_with` adds them all and installs
the error handler. `main.amain()` raises `NotImplementedError` at the
bot-construction seam.

Phase 14 wires the bot. Scope is locked by
`baton-runner/br-2026-05-27-phase-14/STATE.md` AC1–AC6 + signoff decisions
Q1–Q4. Per the Phase-13 digest, this phase MUST honour 7 carry-forward items
(direct-attribute assignment for hooks, `AllowedMentions.none()` on every
reply path including the new liquidation notifier, type-ignore budget of 4,
`Container.bind_runtime(bot)` as the chosen wiring path, etc.).

TDD plan, in order: AC2 (`bind_runtime` on Container — needs tests +
implementation in container.py), AC1 (`build_bot` factory), AC3 (replace
NotImplementedError in main.py), AC4 (bot factory smoke test), AC5
(integration test — may fall back to direct cog callback invocation), AC6
(extra container tests).

## Next steps

1. AC6 first (red): add `bind_runtime` tests to `tests/adapters/test_container.py`
   — two stub guilds, assert every task's `_iter_guild_ids` returns both;
   assert LiquidationTask notifier dispatches via system_channel with
   `AllowedMentions.none()`; assert missing system_channel logs WARNING.
2. AC2 (green): implement `Container.bind_runtime(bot)` —
   `_make_liquidation_notifier(bot)` closure + iter_guild_ids swap.
3. AC4 (red→green): write `tests/adapters/discord_bot/test_bot_factory.py`,
   then create `src/friendex/adapters/discord_bot/bot.py::build_bot`.
4. AC3: replace `NotImplementedError` in `src/friendex/main.py::amain`.
5. AC5: `tests/integration/test_full_command_flow.py` — try dpytest, fall
   back to direct cog callback invocation; RECORD choice here.
6. Run `scripts/gate.sh /tmp/phase-14-pregate-self/` and record results.

## Open questions / risks

- dpytest may not drive `app_commands.Command` callbacks cleanly. STATE.md
  permits the Phase-11 style stub-Interaction fallback; if so, record here.
- `discord.ext.tasks.loop` cannot be constructed off an unbound `_run` — the
  Phase-9 task wrappers leave `_loop` as a bare annotation. Need to verify
  whether tasks already self-bind in `start()` or whether `bind_runtime` must
  build a `tasks.loop(...)` per task and assign `task._loop = ...` before
  `task.start()`. (Reading base_task.py: `start()` calls
  `self._loop.is_running()` — so `_loop` MUST be bound first. Phase 14 owns
  that wiring.)

## References

- Plan: `docs/04-migration-plan.md` §Phase 14 (lines 765–789)
- State: `baton-runner/br-2026-05-27-phase-14/STATE.md`
- Carry-forward: `baton-runner/br-2026-05-27-phase-13/digest-phase-13.md`
- Container: `src/friendex/adapters/container.py`
- Entry seam: `src/friendex/main.py:52`
- Tasks base: `src/friendex/adapters/tasks/base_task.py`
- Liquidation embed: `src/friendex/adapters/discord_bot/embeds.py:638`
