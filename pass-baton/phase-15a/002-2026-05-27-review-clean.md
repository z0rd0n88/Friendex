# Pass-Baton: Phase 15a review — CLEAN

**Date:** 2026-05-27
**Scope:** phase-15a
**Branch:** feat/phase-15a-fixtures
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-15
**HEAD:** 3a90e2c feat(phase-15a): realistic JSON fixtures + integration test

## Verdict

**CLEAN.** Gate green, intent met, tests non-tautological (RED-confirmed),
migrator byte-identical to `f1e0e7f`, no new dependencies.

## Gate result

`scripts/gate.sh baton-runner/br-2026-05-27-phase-15/gate-phase-15a-iter-1/`
→ `GATE: PASS` (all of pytest / ruff-check / ruff-format / mypy green).
Full suite: 787 passed (786 baseline + 1 new).

## Acceptance verification

### A1 — realistic fixtures

- Schema parity vs `tests/fixtures/json/{users,prices,funds,fund_penalties}.json`
  is **exact** (same top-level keys per record in every file).
- `users.json`: 50 entries, ids `1001..1050`. 35 with ≥1 long, 25 with ≥1
  short, 6 frozen shorts. Activity buckets fully populated (`today` + `week`)
  for every user — all 9 required keys (`text_msgs`, `media_msgs`,
  `voice_minutes`, `voice_unique_channels`, `reaction_count`, `reply_count`,
  `role_ping_joins`, `role_ping_join_minutes`, `timestamp`) present.
  `daily.streak` spans `{0,1,2,3,5,6,7,10,14,21,30}` — every spec-required
  value `{0,1,3,6,7,14,30}` is included. All long/short `target_id`s exist in
  both `users.json` AND `prices.json` (zero orphans, as 15a requires).
- `prices.json`: 50 entries, all 5 required keys per record, 252 total
  history points, every stock has ≥3 points.
- `funds.json`: 31 entries (30 funds + `events_wallet`). 12 funds with ≥2
  investors (spec required ≥10). All manager ids and investor ids exist in
  `users.json` (except `events_wallet`'s own `manager_id: "events_wallet"`).
- `fund_penalties.json`: 10 entries. APRs in `{0.05, 0.10}` only.
  `penalty_until` range `2026-06-01T03:00:00..2026-06-08T22:00:00` — all
  future relative to today (2026-05-27), naive UTC (parser localises).

### A2 — integration test

- `tests/integration/test_migration_realistic.py`: `@pytest.mark.asyncio`,
  `sqlite+aiosqlite:///:memory:`, `guild_id="999"`.
- (a) Counts derived from JSON via `_expected_counts` (structural, not
  magic), with shape pins (`users==50`, `stocks==50`, `hedge_funds==31`,
  `fund_penalties==10`) as the regression net.
- (b) 5 read-side methods exercised: `UserRepo.list_all` (count + cash
  spot-check on 3 ids), `FundRepo.list_all` (count + investor count for a
  ≥2-investor sample), `PriceRepo.list_all` (count + `current` for 3 ids),
  `PriceRepo.get_history` (length match), `PenaltyRepo.list_all` (count +
  APR + UTC-aware `penalty_until` round-trip).
- (c) Re-runs `migrate()` on the **same** engine + sessionmaker; asserts
  `second == first` AND the live `list_all` counts still match `expected`
  (this second clause is what makes idempotency non-vacuous — `migrate()`'s
  return dict is "records processed" and would trivially equal regardless,
  but the live-row-count re-check would catch duplicate inserts).

### Migrator unchanged

`git diff f1e0e7f...HEAD -- src/friendex/adapters/persistence/migrate_json_to_sqlite.py`
→ 0 lines. Byte-identical. Phase 15a scope contract honoured.

### RED-under-mutation

Ran three in-place mutations under revert:

1. Delete user `1001` → **RED** at line 109 (`assert expected["users"] == 50`).
2. Drop one non-`events_wallet` fund → **RED** at line 111 (`assert
   expected["hedge_funds"] == 31`).
3. Mutate `1001.cash_balance` to `99999.99` → **GREEN**. Expected — both
   sides re-derive from the same JSON, so the migrator's round-trip is
   structurally validated only. The shape pins (above) are what catch
   fixture-content drift; the round-trip clauses catch migrator-side
   regressions (Decimal quantisation, tz handling, etc).

The work baton's claim that `FileNotFoundError` constitutes RED-first is
weaker than these in-place mutations, but together with the shape pins the
overall regression net is sound.

## Findings

### CRITICAL / HIGH

None.

### MEDIUM

None.

### LOW

1. `tests/integration/test_migration_realistic.py:135,155,171` — wraps
   already-`Decimal` values in `Decimal(str(...))`. Since `_load` uses
   `parse_float=Decimal`, `users[sample_id]["cash_balance"]` is already a
   `Decimal`; `Decimal(str(Decimal('17099.36')))` is a no-op round-trip.
   **Fix (optional):** drop the wrapping — `expected_cash =
   users[sample_id]["cash_balance"]`. Defensive but redundant; not blocking.

2. Fixture history points are not consistently ~24h apart (sample `1003`
   has 7 points ~3.4h apart, `1001` has 8h then 16h gaps). The acceptance
   said "~24h apart" but the migrator imposes no spacing requirement and
   spacing is not asserted in the test. **Fix (optional, if spacing matters
   for downstream tests):** regenerate `prices.json` with a 24h cadence —
   but as-is the fixtures still satisfy "≥3 datapoints", and the spacing
   detail is cosmetic. Not blocking.

3. Migrator return-dict equality (test line 182) is informationally
   redundant — `migrate()` returns input cardinality, so `second == first`
   is true by construction. The non-vacuous idempotency check is at lines
   189-195 (live `list_all` counts). **Fix (optional):** drop the
   `second == first` assertion or replace it with a comment noting it's a
   sanity check on the public return shape, not the duplicate-insert
   guarantee. Not blocking — the live-count check carries the real load.

## Dependencies

No diff in `pyproject.toml` or `uv.lock`. Nothing to flag.

## Open-questions resolution

The work baton's "Schema interpretation" caveat is correct: the task spec's
`daily_streak`, `last_daily_claim`, `activity.yesterday`, `ping_responses`
do **not** match the migrator's actual contract (`daily.streak`,
`daily.last_claim`, `activity.week`, no ping_responses field). The work
unit correctly chose to follow the migrator's parser; the small fixtures
under `tests/fixtures/json/` confirm this is the schema the rest of the
test suite already relies on. Resolution accepted.

The 31-vs-30 funds count (work baton "Open questions" #2) is also correct
— `events_wallet` is stored via `SqlFundRepository.upsert`, identical to
the small fixture under `tests/fixtures/json/funds.json`.

## References

- Work baton: `pass-baton/phase-15a/001-2026-05-27-realistic-fixtures-ready.md`
- Gate logs: `baton-runner/br-2026-05-27-phase-15/gate-phase-15a-iter-1/`
- Digest (phase exit): `baton-runner/br-2026-05-27-phase-15/digest-phase-15a.md`
- Migrator (unchanged): `src/friendex/adapters/persistence/migrate_json_to_sqlite.py`
- Test: `tests/integration/test_migration_realistic.py`
- Fixtures: `tests/fixtures/json/realistic/{users,prices,funds,fund_penalties}.json`
- Issue: #2 (Phase-15a row)
