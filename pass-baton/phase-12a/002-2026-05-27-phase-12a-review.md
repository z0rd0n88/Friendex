# Pass-Baton: Phase 12a independent review — VERDICT ISSUES

**Date:** 2026-05-27
**Scope:** phase-12a
**Branch:** feat/phase-12a-listeners-simple
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-12
**HEAD:** 22494bc feat(phase-12a): listeners foundation — reaction + member cogs

## Where things stand

**VERDICT: ISSUES.** The gate is RED. `bash scripts/gate.sh
baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-1/` exits non-zero
with `pytest` FAIL (`ruff-check`, `ruff-format`, `mypy` all PASS — overall
`GATE: FAIL`). Two tests in
`tests/adapters/discord_bot/listeners/test_member_listener.py` fail because
`MemberListener.on_member_update` passes the wrong kind literal to
`DisciplineService.apply_discipline_penalty`:

```
FAILED test_on_member_update_fires_timeout_on_none_to_set
  Expected: apply_discipline_penalty('42', 'timeout')
    Actual: apply_discipline_penalty('42', 'ban')

FAILED test_on_member_update_passes_kind_timeout_literal
  assert 'ban' == 'timeout'
```

722 other tests pass. The completion baton (`001`) overclaimed "all eight
A1–A8 green" — A5 is violated by the product code. The product bug is at
`src/friendex/adapters/discord_bot/listeners/member_listener.py:65`:

```python
await discipline_service.apply_discipline_penalty(str(after.id), "ban")
```

The docstring at lines 7-10 and 52-58 correctly describes `"timeout"`; only
the literal at the call site is wrong. Tests already pin both the
`assert_awaited_once_with("42", "timeout")` form (line 76) and the
positional-arg form (line 237) — they are load-bearing and would catch
either reversion.

Per the review procedure, with the gate broken, deep code-review and the
prescribed 3-mutation suite were skipped — "no deep review needed on broken
code." A short continuity sweep was still run to surface anything
adjacent:

- **No discord import leak** — `grep '^import discord\|^from discord'` over
  `src/friendex/domain`, `src/friendex/application`,
  `src/friendex/adapters/persistence`, `src/friendex/adapters/tasks`
  returns empty. Listeners are the only new discord-importing surface
  (Phase 10 invariant honoured).
- **No try/except in listeners** — `grep` confirms only a docstring
  occurrence in `reaction_listener.py:19` ("`try/except`" in prose).
  DomainError propagation is preserved (Phase 11a §3, Phase 12 signoff #5).
- **No module-level constants** — both listener files clean (Phase 11a §7).
- **Per-guild factory ctor shape** — both `ReactionListener` and
  `MemberListener` take `*_service_factory: Callable[[str], TService]`
  kw-only, resolve `factory(str(guild.id))` at event time. Matches Phase
  11a §1 and Phase 9 service-factory convention.
- **DisciplineService call shape** — Phase 8f digest pins
  `apply_discipline_penalty(user_id: str, kind: Literal["timeout","ban"])`.
  The call passes positional `str(after.id)` and a positional literal,
  matching the contract — only the literal value is wrong.
- **No new dependencies** — `git diff ea4b7b2..HEAD -- pyproject.toml
  uv.lock` returns empty.
- **Reaction listener** — bot-skip (`user.bot`), self-skip (`user.id ==
  reaction.message.author.id`), and DM guard (`guild is None`) all
  present; delegates `record_reaction(user_id=...)`. Looks correct on
  inspection; not mutation-verified this round (gate broken).

## Findings

### CRITICAL — kind literal wrong on the timeout branch

- **File:** `src/friendex/adapters/discord_bot/listeners/member_listener.py:65`
- **Issue:** The `None → set` `timed_out_until` branch passes `"ban"`
  where A5 + the docstring require `"timeout"`. Two tests RED; gate
  blocks.
- **Fix:** Change the literal.
  ```python
  await discipline_service.apply_discipline_penalty(str(after.id), "timeout")
  ```
- **Re-run after fix:** `bash scripts/gate.sh
  baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-2/` (new
  iter directory) — expect 724/724 green.

No HIGH / MEDIUM / LOW findings raised this round (gate-broken state
short-circuits the deep pass; re-review on iter-2 will run the full
mutation suite and code-review/security-review skills).

## Next steps

1. Apply the one-character fix at
   `src/friendex/adapters/discord_bot/listeners/member_listener.py:65`
   (`"ban"` → `"timeout"`).
2. Re-run the gate into a fresh
   `baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-2/` directory.
3. Re-run independent review (mutations M1/M2/M3, code-review,
   security-review) on the re-greened tree.

## Open questions / risks

- None. The bug is mechanical and the tests pin the fix.

## References

- Acceptance criteria: `baton-runner/br-2026-05-27-phase-12/STATE.md`
  (phase-12a, A5 specifically — lines 76-81)
- Work-unit completion baton:
  `pass-baton/phase-12a/001-2026-05-27-phase-12a-complete.md`
- Gate logs:
  `baton-runner/br-2026-05-27-phase-12/gate-phase-12a-iter-1/pytest.log`
- Failing tests:
  `tests/adapters/discord_bot/listeners/test_member_listener.py:76, 237`
- Phase 8f kind-literal contract:
  `baton-runner/br-2026-05-25-phase-8/digest-phase-8f.md`
