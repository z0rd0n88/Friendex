# Pass-Baton: Phase 12a listeners foundation — kickoff

**Date:** 2026-05-27
**Scope:** phase-12a
**Branch:** feat/phase-12a-listeners-simple
**Worktree:** /home/user/Friendex/.claude/worktrees/br-2026-05-27-phase-12
**HEAD:** ea4b7b2 chore(phase-11): apply review follow-ups (LOWs, non-blocking) (#59)

## Where things stand

Phase 12a slice begins: listener package foundation +
`ReactionListener` + `MemberListener`. Worktree is clean, gate (pytest +
ruff + mypy) is green at HEAD. `src/.../listeners/__init__.py` already
exists as an empty package scaffold; `tests/.../listeners/` doesn't exist
yet. Phase 11 cog conventions (per-guild factory ctor, `guild_id_of`,
`DomainError` propagates uncaught, callback-direct test idiom,
mutation-hardened tests) carry forward into listeners.

## Plan (TDD per AC)

For each AC, write the failing test first, capture RED output, implement
to GREEN. AC ordering:

1. A1/A2 — package `__init__.py`s
2. A3 — `tests/.../listeners/conftest.py` with `fake_message`,
   `fake_member`, `fake_voice_state` + service AsyncMocks/factories
3. A4 — `reaction_listener.py` (self-skip + bot-skip + factory routing)
4. A5 — `member_listener.py` (timeout None→set guard; ban; reason kind)
5. A6 — mutation-hardened test bar (3 specific mutations must each fail
   at least one test under revert)
6. A7 — DomainError propagation tests (no try/except)
7. A8 — gate green: `pytest`, `ruff check`, `ruff format --check`,
   `mypy`; ≥80% line cov on the two listener files + conftest

## Next steps

1. Create `tests/adapters/discord_bot/listeners/__init__.py` and
   `tests/adapters/discord_bot/listeners/conftest.py` (A2 + A3).
2. Write `test_reaction_listener.py` (RED), then `reaction_listener.py`
   (A4).
3. Write `test_member_listener.py` (RED), then `member_listener.py` (A5).
4. Run the gate command: `uv run pytest tests/adapters/discord_bot/listeners/ --cov=src/friendex/adapters/discord_bot/listeners --cov-fail-under=80`.

## Open questions / risks

- Listener events don't carry an `Interaction` — they carry `Reaction`,
  `User`, `Member`, `Guild`. So `guild_id_of` from cogs is NOT reused;
  each listener resolves guild_id from the event (`reaction.message.guild`
  / `member.guild` / on_member_ban's `guild`).
- For 12a the conftest pre-seeds 12b's service fixtures
  (voice_ping_service, etc.) per the brief's instruction; do not wire
  them into 12a's listeners.

## References

- Spec: docs/04-migration-plan.md §Phase 12 (lines 702-732)
- STATE: baton-runner/br-2026-05-27-phase-12/STATE.md (not yet present in
  worktree; per-brief restate above)
- Conventions: baton-runner/br-2026-05-26-phase-11/digest-phase-11a.md
- DisciplineService contract: baton-runner/br-2026-05-25-phase-8/digest-phase-8f.md
- Composite lock keys: baton-runner/br-2026-05-25-phase-8/digest-phase-8a.md
- Issue #2 (phase tracker)
