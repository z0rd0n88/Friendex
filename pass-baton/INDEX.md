# Pass-Baton Index

Latest entry per scope. Read the linked file for full context. **New sessions should start here.**

| Scope | Latest | Date | Topic |
|---|---|---|---|
| phase-3-domain | [001](./phase-3-domain/001-2026-05-15-phase-3-pr11-ready.md) | 2026-05-15 | Phase 3 code-complete; PR #11 open, awaiting review |
| phase-4-domain-funcs | [003](./phase-4-domain-funcs/003-2026-05-23-phase-4-review.md) | 2026-05-23 | Independent review — VERDICT CLEAN (gate green, mutation-verified); 2 MEDIUM + 2 LOW non-blocking findings; digest written |
| phase-5-orm | [003](./phase-5-orm/003-2026-05-24-fk-decision-closed.md) | 2026-05-24 | FK enforcement decision closed — PRAGMA foreign_keys=ON chosen (ADR-0002); PR #36 open |
| phase-6-repos | [015](./phase-6-repos/015-2026-05-25-hardening-h1-h2-review.md) | 2026-05-25 | Independent review of hardening fixes H1 (N+1 batching) + H2 (MigrationError) — VERDICT CLEAN; gate green, both RED-verified under reversion; 1 LOW (weak ordering test) + 1 INFO, no CRITICAL/HIGH/MEDIUM |
| phase-7-locks | [000](./phase-7-locks/000-2026-05-25-phase-7-merged-carryforward.md) | 2026-05-25 | Phase 7 LockManager merged (PR #38, `97b505e`); VERDICT CLEAN, 100% cov. Carry-forward: 1 MEDIUM (cancel-mid-acquire lock leak) to fix in Phase 8a + 1 LOW; conventions for service lock usage |
| phase-8-fakes | [002](./phase-8-fakes/002-2026-05-25-review-clean.md) | 2026-05-25 | Independent review — VERDICT CLEAN; gate green, boundary tests RED-verified (non-tautological), semantics match real adapters + Phase 6 digests; 1 LOW (get returns mutable ref) + 1 INFO, no CRITICAL/HIGH/MEDIUM; digest written |
| phase-8a | [005](./phase-8a/005-2026-05-25-phase-8a-review-iter2-clean.md) | 2026-05-25 | Re-review iter-2 — VERDICT CLEAN; gate green (430 pytest, ruff/format/mypy), iter-1 HIGH (composite lock key) resolved by load-bearing two-guild isolation test + surgical 6-call-site fix, both deferred LOWs documented + untouched, no new deps. Digest written |
| phase-8b | [004](./phase-8b/004-2026-05-25-phase-8b-review-iter2-clean.md) | 2026-05-25 | Re-review iter-2 — VERDICT CLEAN; gate green (445 pytest +6 from baseline, ruff/format/mypy), iter-1 H1 (RMW race) resolved with load-bearing `_BarrierPriceRepo` test, M2 (history append + ATH ratchet) resolved with 5 non-tautological tests, M1 (K placeholder) deferred-as-documented per contract, L1/L2/L3 all addressed. Original 5 ACs (B1-B5) still hold. No new deps. Digest updated post-fix |
| phase-8c | [001](./phase-8c/001-2026-05-25-review-iter-1-clean.md) | 2026-05-25 | Independent review iter-1 — VERDICT CLEAN; gate green (486 pytest, ruff/format/mypy), 92.53% cov on `trading_service.py`, all 14 ACs load-bearing (C9 boundary partial — within-cooldown rejection pinned, after-cooldown success not). 0 CRITICAL/HIGH; 2 MEDIUM (M1 after-cooldown test gap, M2 `ITradeCooldownRepo.get` Protocol lacks `now=` kwarg — functionally safe but contract drift) + 2 LOW. Digest written |
| phase-8d | [002](./phase-8d/002-2026-05-25-review-iter-1-clean.md) | 2026-05-25 | Independent review iter-1 — VERDICT CLEAN; gate green (500 pytest, ruff/format/mypy), all D1–D10 load-bearing with concrete numbers, read paths confirmed lockless, `capture_month_start_net_worth` takes one per-user `locked()` inside the loop, 24h boundary inclusive `>=` matches Phase-6 adapter semantics, all 4 read-models `frozen=True`, no new deps, no `Settings` change, no `ITradeCooldownRepo` touch. 0 CRITICAL/HIGH/MEDIUM; 1 LOW (lazy Decimal import) |
| phase-8e | [002](./phase-8e/002-2026-05-25-review-iter1-clean.md) | 2026-05-25 | Independent review iter-1 — VERDICT CLEAN; gate green (522 pytest, ruff/format/mypy), all E1–E11 load-bearing (E11 simulates 7 actual claims; E6 asserts exact Decimal accrual proving `compute_apy_accrual` is delegated; E4 stacks penalty APR 0.05→0.10), composite lock keys + read-inside-lock RMW + sweep-per-fund + 2-key `locked()` all verified, `AlreadyClaimedToday` correctly placed under `DomainError`, `invest()` raises `NotImplementedError` per §Open-Q5, no new deps, no new `Settings`. 0 CRITICAL/HIGH/MEDIUM; 2 LOW (zero-balance fund side-effect on insufficient-withdraw path + `_get_or_create_account` uses `datetime.now` instead of threaded `now`) |
| phase-8f | [002](./phase-8f/002-2026-05-25-review-iter1-clean.md) | 2026-05-25 | Independent review iter-1 — VERDICT CLEAN; gate green (180 application pytest, ruff/format/mypy), 92.68% application cov (≥ 85% gate), 8c suite 41/41 pass (no regression). All F1–F8 load-bearing with concrete numbers (F1 149 ≠ liquidate, F2 exactly 150 liquidates, F3 frozen short liquidates via full sweep entry point, F4 every payload field, F5/F6 exact $83/$166, F7 $75→$70 floor equality, F8 opt-out still penalised). Lock composition design (a) confirmed in code — no nested `locked()` on non-reentrant manager. `_cover_internal` private; public `cover()` signature has no `force` param. 8b RMW pattern correctly mirrored in `DisciplineService`. Composite `f"{guild}:{user}"` lock keys everywhere; per-short sweep locking. No new deps. Carry-forwards (8b M1, 8c M2, 8a 2 LOWs) untouched per contract. 0 CRITICAL/HIGH/MEDIUM/LOW |
| phase-8-followup | [002](./phase-8-followup/002-2026-05-25-review-clean.md) | 2026-05-25 | Independent review of the two-chore follow-up unit — VERDICT CLEAN; gate green (pytest/ruff-check/ruff-format/mypy via gate-phase-8-followup-iter-1). Chore A (`68c1a8f`, widen `ITradeCooldownRepo.get` with kwarg-only `now=`): Protocol + SQL adapter + both fakes match; RED-first `inspect.signature` test is load-bearing; behavioural twin pins expired-row filter; `_check_cooldown` drops the compensating in-service expired guard; docstring captures the active-vs-expired contract. Chore B (`b01480b`, `activity_tick_k` 0.5 → 0.3): docstring no longer says "TBD/placeholder", carries 2026-05-25 rationale + Phase-9 re-tune note; all `test_price_tick_service.py` assertions stay K-agnostic relative comparators (no test loosened). Commits cleanly separable, exact template messages, no dep changes. 0 CRITICAL/HIGH/MEDIUM; 1 LOW-as-note (`activity_tick_k` typed `float` not `Decimal` — correct given coefficient semantics). |
| phase-9 | [003](./phase-9/003-2026-05-25-phase-9-review-iter1.md) | 2026-05-25 | Independent review iter-1 — VERDICT CLEAN; gate green (581 pytest, ruff/format/mypy via gate-phase-9-iter-1/), 100% cov on `src/friendex/adapters/tasks/` (10 modules), all 11 ACs load-bearing (mutation-think confirmed on 4 sampled tests: gate-revert / order-swap / survivor-swap-back / `try/except`-drop), per-guild fan-out N=2 demonstrated in 6 sweep-task tests, reset tasks use N=1 (boundary semantics are the load-bearing rule), `SystemState` shape unchanged (no new field), no `discord` import anywhere in `adapters/tasks/` (cadence-as-declaration design pulls AC3 policy package-wide), `freezegun` already on `origin/main` (NOT a new dep), no `pyproject.toml`/`uv.lock` changes, containment honoured (only `src/friendex/adapters/tasks/` + `tests/adapters/tasks/` + baton/digest metadata). Phase 8e carry-forwards untouched. 0 CRITICAL/HIGH/MEDIUM/LOW; 2 INFO notes (`_safe_run` `Awaitable[Any]` widening — intentional per docstring; reset-task N=1 — acceptable per work-unit spec). Phase-exit digest written |
| phase-10 | [001](./phase-10/001-2026-05-26-phase-10-review-clean.md) | 2026-05-26 | Independent review iter-1 — VERDICT CLEAN; gate green (616 pytest, ruff/format/mypy via gate-phase-10-iter-1/), 100% branch coverage on `embeds.py` (128 stmts / 10 branches), all 8 ACs load-bearing (AC3 palette 5 distinct ints, AC7 money fails on `,` or `.2f` drop, AC8 `description == user_facing_message` verbatim — see L1 for stricter mutation), `build_fund_info_embed` kw-only enforced (positional call → TypeError), `PortfolioSnapshot` uses all 6 fields, Phase 9 invariant preserved (zero `import discord` in `adapters/tasks/`; embeds.py is the only `discord`-importing source file), no new deps. 0 CRITICAL/HIGH/MEDIUM; 2 LOW (L1 `str(error)` mutation not caught — coincidentally equal to `user_facing_message` today; L2 `_money` of negative renders `$-50.00` — unreachable today) + 3 INFO (double-bold fund name, unsanitised fund name in title/description → Phase 11 cogs must set `AllowedMentions.none()`, empty `__init__.py` intentional). Phase-exit digest written |
| phase-11a | [003](./phase-11a/003-2026-05-26-phase-11a-review.md) | 2026-05-26 | Independent review iter-1 — VERDICT CLEAN; gate green (649 pytest, ruff/format/mypy via `gate-phase-11a-iter-1/`), 100% line+branch cov on each new cog file (≥80% gate satisfied), all 8 ACs (A1-A8) load-bearing — reviewer ran 3 live mutations under revert and confirmed each fails the matching test: (1) `/daily` catches `DomainError` → `test_daily_propagates_already_claimed_today` fails; (2) `/optin` hard-codes `False` → `test_optin_calls_set_opt_in_true` fails; (3) drop `@has_permissions(manage_guild=True)` on `/game_intro` → `test_game_intro_has_manage_guild_permission_check` fails. Continuity preserved: zero `discord` imports in `domain/`/`application/`/`adapters/persistence/`/`adapters/tasks/`, no module-level constants in cogs, `COLOR_*` reused from Phase 10 embeds, no new deps, `Decimal`+UTC invariants intact. 0 CRITICAL/HIGH/MEDIUM; 2 LOW non-blocking carry-forwards (L1 prose glitch in `admin_cog.py:10-11`; L2 `type: ignore[no-untyped-def]` proliferation in cog tests). Phase-11a-exit digest written |
| phase-11b | [002](./phase-11b/002-2026-05-26-phase-11b-review.md) | 2026-05-26 | Independent review iter-1 — VERDICT CLEAN; gate green (669 pytest, ruff/format/mypy via `gate-phase-11b-iter-1/`), 100% line+branch cov on each new cog file (≥ 80% gate satisfied), all 5 ACs (B1-B5) load-bearing — reviewer ran 3 live mutations under revert and confirmed each fails the matching test: (1) `/portfolio` drop `ephemeral=True` → 2 tests fail; (2) `/price` swap `build_price_embed` → `build_mystats_embed` → 2 tests fail with diagnostic `AttributeError`; (3) `/mystock` drop `@app_commands.command` decorator → 5 tests fail. Continuity preserved: zero `discord` imports in `domain/`/`application/`/`adapters/persistence/`/`adapters/tasks/`, no module-level constants, `COLOR_NEUTRAL` reused from Phase 10 embeds, no `try/except DomainError`, no new deps, `Decimal`+UTC invariants intact, service signatures match Phase 8d digest. 0 CRITICAL/HIGH/MEDIUM; 1 LOW (inconsistent positional-vs-kw call style for `portfolio_snapshot` between 11a `AccountCog.balance` and 11b `PortfolioCog.portfolio` — optional 11c cleanup) + 2 INFO (Phase 10 I2 `AllowedMentions.none()` carry-forward lands in 11c `fund_cog`; pre-existing test-local `import discord.app_commands` pattern mirrors 11a). Phase-11b-exit digest written |
| phase-11c | [002](./phase-11c/002-2026-05-26-phase-11c-review.md) | 2026-05-27 | Independent review iter-1 — VERDICT CLEAN; gate green (703 pytest, ruff/format/mypy via `gate-phase-11c-iter-1/`), 100% line+branch cov on each new cog file (≥80% gate satisfied), all 5 ACs (C1-C5) load-bearing — reviewer ran the 4 prescribed mutations under revert and confirmed RED on 3 (mutation 1 fails 2 tests on `/fund info`; mutation 2 fails `/buy` test via diagnostic AttributeError on `total_revenue`; mutation 3 silent-catch of NotImplementedError fails `/fund invest` test). Mutation 4 (`Decimal(str(amount))` → `Decimal(amount)`) stays GREEN because test literals 100.50 and 75.25 are exact dyadic fractions — recorded as LOW-1 (not blocker; brief anticipated this). Continuity preserved: zero `discord` imports in `domain/`/`application/`/`adapters/persistence/`/`adapters/tasks/`, no `try/except DomainError` in cogs, `AllowedMentions.none()` on every send, `COLOR_*` reused from Phase 10, FundGroup subclasses `app_commands.Group` with `FundCog.group` wired for Phase 13, no new deps. 0 CRITICAL/HIGH/MEDIUM; 3 LOW non-blocking (L1 dyadic test literals, L2 APY assertion weakness, L3 ARCH.md stale post-commit) + 0 INFO. Phase-11c-exit digest written |
| phase-12a | [003](./phase-12a/003-2026-05-27-phase-12a-review.md) | 2026-05-27 | Independent review iter-1 retry — VERDICT CLEAN; gate green on clean tree (724 pytest, ruff/format/mypy via `gate-phase-12a-iter-1-retry/`), 100% line+branch cov on `reaction_listener.py` + `member_listener.py` + listeners `__init__.py` (≥80% gate satisfied), all 8 ACs (A1-A8) load-bearing — reviewer ran 3 prescribed mutations under revert and confirmed each fails the matching test: M1 (drop `user.bot` skip) → `test_on_reaction_add_skips_bot_reactor` FAIL; M2 (drop `before.timed_out_until is None` guard) → `test_on_member_update_does_not_fire_on_extension` FAIL; M3 (flip `"timeout"` ↔ `"ban"` at both call sites) → 4 tests FAIL across update + ban literal pins. Supersedes iter-1 baton 002 — that ISSUES verdict was a false positive (the 002 reviewer applied M3 then misread its own un-reverted mutation as a product bug). HEAD `member_listener.py:65` correctly passes `"timeout"`. Continuity preserved: zero `discord` imports in `domain/`/`application/`/`adapters/persistence/`/`adapters/tasks/`, no `try/except` in listeners, per-guild factory ctors, no module-level constants, `Decimal`+UTC invariants intact, kind literals `"timeout"` and `"ban"` match Phase 8f contract, no new deps. 0 CRITICAL/HIGH/MEDIUM/LOW. Phase-12a-exit digest written |
| phase-12b | [003](./phase-12b/003-2026-05-27-phase-12b-review.md) | 2026-05-27 | Independent review iter-1 — VERDICT CLEAN; gate green on clean tree (750 pytest, ruff/format/mypy via `gate-review-12b-iter-1/`), all B1-B8 load-bearing — reviewer ran the 4 prescribed mutations under revert: M1 (drop `author.bot` skip) → `test_on_message_skips_bot_author` FAIL; M2 (flip SWITCH order) → `test_on_voice_state_update_switch_finalises_old_before_new` FAIL with `leave_index=1, join_index=0`; M3 (revert CF-1 to in-place `.add`) → `test_link_ping_rebuilds_session_immutably` FAIL with `frozenset({4242}) == frozenset()`; M4 (drop CF-2 lock + inner re-read, use outer snapshot) → `test_concurrent_responders_respect_cap_under_per_ping_lock` FAIL with both stocks at $120. Continuity preserved: zero `discord` imports in `domain/`/`application/`/`adapters/persistence/`/`adapters/tasks/`, no `try/except` in listeners, composite lock key `f"{guild}:ping:{msg_id}"` matches Phase 8a precedent, `VcBoostTask.set_store_for_guild` seeding contract honoured (Phase 9 §3), VC-ping rule matches `original-skeleton.md:494-503`, no new deps. 0 CRITICAL/HIGH/MEDIUM; 2 LOW non-blocking carry-forward (L1 CF-2 mutation pin is partial — inner re-read alone defeats the race even without the LockManager lock; L2 `_BarrierPingSessionStore._barrier_fired` semantics need a doc comment for reuse). Phase-12b-exit digest written |
| phase-13 | [001](./phase-13/001-2026-05-27-gate-green-red-backfill.md) | 2026-05-27 | Phase 13 (error handler + container) gate green (771 pytest, ruff/format/mypy); resume-unit fixed ruff/mypy breakage left by silent prior unit (overload narrowing in error_handler `_reply`, Callable return types on 10 factory builders, ambiguous `l` loop var, TC002/TC005/E501 cleanups); backfilled RED captures for AC2/AC5 (combined), AC3 (mypy+import surface — no runtime test by signoff design), AC6 (AttributeError on package attr without re-export); 3 type:ignore added (method-assign for sanctioned `bot.tree.on_error`, call-arg for env-driven `Settings()`) — each one-line + justified; `_reply` split into `_reply_embed` + `_reply_content` to satisfy discord.py overload narrowing without further ignores; no new deps; ready for review |

*Top-level seed: [`000-2026-05-15-start-pass-baton.md`](./000-2026-05-15-start-pass-baton.md) (empty placeholder; establishes the sequence head).*

---

## How to read this

- Each row points at the **highest-numbered** pass-baton in that scope's subdirectory.
- Older pass-batons in the same scope are reachable by following the `## References` section of the latest one (pass-batons link backward as they evolve).
- The index is overwritten by the `pass-baton` skill on every write. Do not hand-edit.
